# Memento

## Status

This project has only been scaffolded. Do not infer product requirements before the project briefing is added.

## Development

* Support Python 3.10 and newer.
* Keep runtime dependencies empty unless the briefing provides a concrete need.
* Use the `src/memento/` package layout.
* Add regression tests for every behaviour change.
* Run `make check` before committing.
* Follow YAGNI principles.

## Git

* Never rebase; merge or use `git pull --no-rebase`.
* Commit as `Rui Carmo <rui.carmo@gmail.com>`.
