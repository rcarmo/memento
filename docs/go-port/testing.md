# Test gates for the Go port

## Coverage is a gate, not a completion claim

Every implemented Go package must have zero uncovered statements in `go test -coverpkg=./...` output. The gate checks raw counters rather than the rounded `100.0%` display, with no excluded production files or helper allowlists. CLI entry wiring is tested too. CI runs the same gate on amd64 and ARM64; cross-compilation alone does not prove execution correctness.

Go reports statement coverage, not full branch/condition coverage. Explicitly enumerate alternate conditions, invalid types, missing fields, empty values, nulls, malformed/truncated frames, overflow, cancellation, shutdown and concurrency. Add each observed regression before changing code. Packages do not count as ported solely because their current small implementation has full coverage.

Required per-slice commands:

```sh
make -C go check
make -C go race
make -C go fuzz
make -C go cross
```

The race detector's build needs cgo on the test runner; distributed binaries and normal test builds use `CGO_ENABLED=0`. Check runtime dependencies in packaging tests. Fuzzing uses a fixed 10,000 generated executions per target with a separate 120-second safety timeout; this avoids a wall-clock shutdown race observed on a loaded native CI runner. Longer scheduled corpus runs are still required. Fuzzing starts with small bounded runs for CI and needs longer scheduled corpus runs as parsers/transports are ported. No untrusted fixture can trigger unbounded allocation or leak the daemon's resources.

## Reference generation

The runtime and ordinary Go tests do not need Python or Rust. The separate test oracle does. It imports the actual pinned Python/uMCP implementation and builds the existing Rust vector crate to emit synthetic results.

```sh
make -C go oracle \
  ORACLE_PYTHON=/path/to/reference/venv/bin/python \
  UMCP_REFERENCE=/path/to/umcp-at-pinned-commit
```

`oracle/export.py` refuses mismatched uMCP HEAD or modified reference source. Memento source is compared against the recorded baseline, including uncommitted changes. Rust reference builds must pass this preflight as part of the combined target. The oracle files are never part of the shipped Go runtime.

Check in and review regenerated fixtures; ordinary CI must never regenerate expected values from the candidate implementation. Do not use production content, credentials or private model prompts. Public model binaries may be fetched through an explicit opt-in test-asset step with pinned SHA-256; they must not become opaque CI downloads from a moving tag.

Current captures:

* Python envelopes: defaults, null data, warnings, stale revisions, error fields.
* Repository paths: 37 synthetic fixtures compare canonical syntax, reserved names, read/write checks, symlink parents/targets, FIFO/directory targets, missing files and error text. Extra tests inject OS failures and preserve pathlib root semantics (`..` is not lexically collapsed across symlinks). Fuzzing covers validation/classification. Race-safe opens and filesystem mutation/recovery are separate, unfinished gates.
* Concept metadata: 353 schema cases and nine body-normalisation cases compare Pydantic field coercion, timestamps, tuple sorting/deduplication and control characters. Frontmatter adds 145 parse and 253 exact serialization cases for YAML/JSON detection, aliases/merges, malformed input, duplicate keys, scalar quoting, Unicode breaks and long plain/single-quoted lines. Fixture metadata records Python library versions. Default omitted status retains the source enum serialization failure; numeric timestamp strings and Python floats retain different negative-fraction behaviour. Pure-Go YAML node parsing is bounded to depth 100 and 100,000 expansions; these resource caps differ from Python. Full graph/key/type fidelity, wide double-quoted wrapping and broader format corpora remain open. Imported dependency internals are outside Memento's statement profile; all implemented Memento Go counters still must be covered.
* Markdown links: 118 documents compare structural href/text/block-line extraction and parser-confirmed rename edits, plus 12 external-scheme and six changed-destination cases. Fixtures cover references, images/nested links, code exclusion, unsafe schemes, entities and common URI/punycode normalisation. Goldmark/mdurl obscure edge equivalence needs a wider corpus; empty rename source is rejected as a no-op instead of the source's nonterminating loop. Bundle fixtures add ten filesystem/filter/audit scenarios and three exact generated-output datasets. Extra tests cover symlink-directory classification, disappearing/unreadable files and sort ties. Python's set iteration can vary among case-fold-equal index entries; Go chooses lexical ties explicitly. Race-safe filesystem opens remain unimplemented.
* Writer leases compare six owner/contender fixtures and retain the advisory `flock` inode contract. Tests cover competing goroutines, a separate process, abrupt child termination/reacquisition, invalid UTF-8 diagnostics and acquisition/release I/O failures. No lock-file unlinking or PID-based stale-lock override is introduced. This verifies local Linux filesystem exclusion, not NFS/SMB lock behaviour or Git/SQLite recovery.
* Git store slice uses a deterministic Python/Git-generated packed bare fixture with executable/symlink entries, addition/change/deletion diffs and source CAS outcomes. Tests verify packed reads, missing/corrupt tree failures, lock contention, native Git competing updates (one winner), native fsck and injected publication I/O failures. Go-git's filesystem CAS is deliberately not used: it locks the ref file, whereas Git uses an exclusive `.lock` and rename. A failed parent-directory sync after publication returns an indeterminate-durability error requiring reconciliation. Reflog-enabled stores, symbolic/symlink main refs, ref deletion and alternate ref storage fail explicitly. Revision input currently requires full SHA-1; raw tree names rather than Git's quoted CLI path presentation are returned. Checkout fixtures now compare raw content, executable bits and symlink targets against Python/Git; tests cover empty trees, stale-cache replacement, partial-write failures, metadata/collision rejection, root-overlap checks and symlink-escape attempts through os.Root. Materialisation can leave a partial derived cache on failure, as in the source; callers must reconcile/rebuild before serving it. Git attributes/filters, complete file-mode/configuration parity and power-loss recovery are unfinished. Host parent directories must remain service-owned; hostile ancestor replacement and power-loss testing remain open.
* Git object writes reproduce packed-reference fixture blob/tree/commit hashes and exact commit bytes with explicit author/committer timestamps. Detached linked worktrees are recognised by native Git; exact-path commits leave unrequested changes out, keep main unchanged, and have a clean native cached diff. Tests cover deleted/subtree paths, modes/links, invalid metadata, lock contention, failed index/HEAD transitions and post-rename lock ownership. New object fsync/power-loss semantics, reflogs, symbolic HEAD, ignore/pathspec/filter/configuration behaviour and source deletion of pre-existing worktree paths are not yet equivalent. Existing operation-state collisions fail closed until recovery is wired.
* Bootstrap reproduces the seeded Python/Git root commit hash, passes native fsck and creates an empty-tree root/current checkout without a seed. Tests cover seed files/modes/internal links, special-file/external-link rejection, current/main preservation and injected setup/copy/object/publication failures. The Go entrypoint requires the caller's writer lease, refuses pre-existing bootstrap state/main, and does not reproduce Python's destructive repeat-bootstrap behaviour. Gitignore/attributes/filter and external seed-symlink compatibility are unfinished.
* Control SQLite: fourteen Python-created database fixtures verify schema v10 and migration snapshots, including duplicate v5 proposal skipping, original manifest/blob preservation, exact sorted/ASCII patch JSON and SHA-256, unsupported versions and partial-DDL rollback semantics. Seventeen operation commands compare records and reopened rows: exact request hashes, principal/key replay, blind transitions, failed retry and result JSON. Tests cover WAL/FK/busy-timeout, independent-handle uniqueness races, invalid rows, cancellation and injected I/O/SQL failures. modernc.org/sqlite v1.38.2 builds without CGo. SQLite engine-version differences, Python nonfinite/surrogate JSON parsing, unrestricted JSON depth and remaining stores/recovery are open gaps.
* Transactions: nine Python checkpoint fixtures compare exact main/worktree revisions, operation rows, callback order, recovered classification, committed changed-path replay and final files. Independent subprocesses are killed at `commit_created` and `publication_complete`, then reopened recovery verifies retry versus replay without a duplicate commit. Tests also cover concurrent Apply calls, optimistic conflicts, failed-operation retry, dirty staging, SQL/Git/derived-update/cleanup errors, callback panics and original-ID preservation on failed DB updates. Cross-process exclusion still requires a writer lease. Go adds worktree retention on indeterminate ref durability and uses a non-reentrant mutex; recursive Apply, non-string replay-path coercion and generic exception-class text differ from Python. Full service/derived-index recovery, persistent orphan cleanup and power-loss/object-durability testing remain open.
* Access/authz: 630 path cases exercise protected grants, root boundaries, trash unwrapping, roles and alias/control rejection. Thirty-two managed-access steps compare synthetic AES-GCM/scrypt verifier blobs, HMAC token digests, deterministic one-time tokens, account transitions, persisted JSON/audit rows and master rotation/reopen. Extra tests cover final-admin protection, sticky idempotency claims on later failure, legacy sandbox rename, SQL rollback, malformed rows, crypto errors and concurrent revoke/read. Store methods rely on the embedding admin service to authorise actors; that wiring is unfinished. Multi-handle final-admin races, complete config model validation, Python's most permissive base64/JSON coercions and out-of-band row corruption need further review. Tests never use live credentials.
* Proposal persistence: nine status/asset fixtures and 45 list-query combinations compare submitted creation, explicit expiry, canonical patch JSON/hash, attached bytes, cursor ordering, unresolved filtering and effective-expired status predicates. Blind status updates clear omitted review/application fields, as Python does. Tests cover duplicate-asset rollback, caller-owned transactions, FK failures, JSON/range errors and deferred constraint failure at COMMIT. A pinned-connection transaction helper explicitly rolls SQLite back after failed COMMIT, since database/sql marks the sql.Tx done before SQLite necessarily ends its transaction; proposal, access and v5-migration regressions verify no leaked partial rows. Service review/apply/rebase validation is unported; staging and queue refresh are covered below.
* Proposal queue refresh: 63 Python cases compare all nine statuses across same-base, clean stale, changed targets, asset-only changes, unavailable history and expiry boundaries. Exact event JSON, before/after records and repeat-refresh history are compared. Go tests add 100-record pagination, shared-diff isolation, ignored bundle errors versus propagated I/O/cancellation, real Git advancement, SQL event/update failures and deferred-COMMIT rollback/retry. Applied/expired records are skipped by queue scans; refresh preserves stored author, patch and review fields. The caller must serialise refresh with review/rebase/apply, which are not implemented here. Go rejects non-string paths instead of Python's permissive str() conversion; malformed-patch diagnostics and arbitrary externally populated diff-cache values are outside these fixtures.
* Proposal validation/access: 181 normalisation, 483 path-policy and 350 visibility cases compare the source. Checks include optional/null/default fields, arbitrary asset metadata pending later validation, 20-item trash-only batches, duplicate targets, rename destinations, protected reads and author/curator ownership. Admin alone does not bypass ownership. Read access precedes write access; model-validation failures propagate while policy failures become denials. Tests cover malformed persisted patches, absent records, concurrent policy isolation and 10,000 fuzz executions for normalisation/policy stability. Inputs are JSON-domain values; bytes, tuples and other Python-only model inputs are not accepted. Pydantic/attribute diagnostics and non-string unsupported-kind formatting remain different. Validated changes must precede calls to the low-level policy helper; trusted request-context wiring and review/rebase/apply are separate work.
* Proposal rebase: 27 Python scenarios compare returned data or policy errors plus every proposal/event/operation field and retained asset metadata/bytes. Cases cover all initial statuses, stale expected revisions, conflicts, archival rejection, skill tags from current content or same-proposal changes, permission checks before replay, replay after repository advancement and injected event/update/journal failures. Refresh is a separate committed transaction: later rejection preserves its system event/status but no rebase event or successful journal. Tests add missing/corrupt rows, random/JSON/SQL failures, deferred-COMMIT rollback/retry and twelve concurrent calls across two service/database handles producing one event/operation. Rebase shares the repository lock with transaction apply/recovery; callbacks cannot recursively acquire it. Replay deliberately accepts any existing operation state as Python does. MCP identity/envelope wiring, review/apply and cross-process service tests remain open; the daemon must hold a writer lease.
* Proposal summaries: 24 Python cases compare exact JSON payloads with and without conflicts. Covers 200-change/asset limits, 2,000-code-point intent truncation, skipped malformed changes, missing fields, unavailable historical bases, and asset/body comparison using the first matching create/patch body or current content. Matching asset paths are sorted without deduplication. Missing/invalid bundle content yields null match status rather than suppressing the asset; unexpected I/O failures propagate. Tests include malformed patch/manifest/entries, public Git-backed summary reads and journal-safe JSON encoding. These summaries do not authorise callers, validate ZIP bytes or generate previews.
* Archival inspection: 19 report/error scenarios and 18 stored-report visibility cases compare Python, using disposable SQLite/file state and synthetic reference callbacks. Inspection requires matching main/index revisions and a ready index, rejects existing trash destinations, checks both permissions, caps inbound references before ACL filtering, and retains accepted assets/history without rewriting links. Tests cover exactly 100 and 101 references/versions, duplicate SQL rows, missing content/index, stale state, unsafe targets, malformed metadata and SQL/iteration failures. Index opens use mode=ro and never create missing files. Stored reports are re-scoped to current read grants, or recomputed for submitted/approved proposals at the current base. Inputs must be normalised; malformed stored-report types fail earlier than Python's truthy recompute shortcut. Derived-index construction, hostile filesystem replacement and full archival mutation remain open.
* Durable review: 56 Python scenarios compare data/error fields and full proposal/event/operation rows for approve/reject/request_changes/invalid decisions. Covers stale/conflicting/expired/applied proposals, mandatory curator/write grants, optional generated keys, changed-request replay conflicts, revoked permissions and archival freshness. Review reads its updated proposal/assets and builds the response inside the same transaction before journalling. Tests inject event/update/readback/asset-query/summary/journal and deferred-COMMIT failures, verify rollback/retry, and exercise twelve concurrent reviews producing one operation/event. Restart replay retains asset bytes and still enforces current permissions without re-reading Git/index data. Full error envelopes and endpoint registration are not part of these internal methods.
* Concept worktree mutations: 27 Python scenarios compare exact file text, directory state and changed-path sets for create/patch/rename/trash, including size, missing/collision, invalid-content and reference-permission failures. A separate copied-metadata serializer retains Python model_copy updates without silently deduplicating/reordering tags or rejecting an empty patch title; new concepts still validate normally. Rooted no-follow writes and exclusive creates reject symlink escapes and successor collisions, intentionally stricter than source direct writes. Injected read/list/mkdir/write/unlink/rename/close failures and short writes are tested. A real pure-Go Git transaction composes the helpers, verifies rewritten links and byte-preserving trash, replays without mutating again, and leaves main unchanged after a partial worktree failure. Callers must supply validated changes, authorised identity, limits, and a private transaction worktree; arbitrary hostile ancestor replacement remains outside this gate. Stored-asset application and aggregate mutation callbacks are covered below; proposal apply orchestration is unfinished.
* Stored-asset worktree application: 14 Python cases compare exact accepted metadata/ZIP bytes, digest error precedence, missing proposals/assets/content, version/kind rejection, duplicate versions and existing asset-backed create-to-patch adaptation. Source application compares digest fields and manifest shape, not ZIP content or stored concept/kind/version identity; preparation/staging must validate those boundaries. Synthetic invalid ZIP bytes explicitly test this responsibility without weakening it silently. Aggregate changes preserve order while returning a sorted path set; a real Git transaction publishes concepts and accepted bytes, then replays without writes. Numeric-string/boolean/integral-float and unbounded-integer Pydantic manifest coercions remain a documented gap in ParseManifest, along with malformed diagnostics.
* Full proposal payloads/previews: 11 preview cases, six direct difflib cases and three payload/history fixtures compare exact source output. Preview patches preserve original updater/time, copy-metadata quirks and first body line ordering; creates use fixed placeholder ID/actor/time. Unified diffs use pure-Go go-difflib v1.0.0 with Python splitlines boundaries, including CRLF/Unicode/control separators and repeated lines beyond the autojunk threshold. Histories retain raw details_json, newest-first event IDs and the 50-row limit; optional absent arrays differ from explicit nulls. Tests cover malformed patches/rows, repository/database failures and public methods. The caller must authorise full payload visibility; broader difflib corpora and response-envelope integration remain open.
* Proposal apply: 18 Python fixtures use real control rows/worktree mutation with a synthetic publisher to compare authorisation/replay/conflict/status order, request hashes and complete successful payloads. Separate real Git tests verify publication, author attribution, derived-update callbacks, archival index freshness, ten concurrent same-key retries, reopened replay and filtered stored replay paths. Fault tests cover prepublication errors, random/JSON/SQL failures, proposal-update failure after successful publication, queue/tracking/payload errors and revoked replay grants. The source does not atomically update proposal state with Git: a succeeded operation can coexist with an old approved proposal after a DB error, and a retry can then conflict. Applied replay also rebuilds its preview and can fail if current content is missing or invalid. Apply shares the outer repository lock with review/rebase; ApplyUnderLock avoids Go's non-reentrant mutex deadlock. The source @_serialized decorator covers the same scope. Derived index construction and optional hot-memory bookkeeping are not implemented; callbacks preserve integration points, not full service parity. HTTP/MCP envelopes and cross-process service recovery still need tests.
* Proposal submission/preparation: 33 Python scenarios compare full control/proposal-asset/staged/event rows and returned payloads; 99 base64 cases pin strict decoding to the current reference. Covers inline/staged archives, ownership, kind/version, rename overlap, missing/excess inputs, canonical skill body/tag/root-file binding and active same-version proposals. Older records refresh and staged expiry commit before later submission rejection; proposal creation, copied assets and staged consumption commit together. SQL insert/asset/consume/deferred-COMMIT and post-commit payload faults are tested. Public pure-Go submit/review/apply publishes the exact staged bytes; two competing submissions yield one consumer and consumed blobs cannot be reused. Plain submissions intentionally generate fresh IDs rather than durable replay keys. Non-string path/kind/version coercion is stricter than Python, and arbitrary manifest-path Unicode escaping still needs review. The source's models-off flow is tested; model-assisted drafting, tool envelope wiring and derived-index construction remain open.
* Proposal get/asset-get: 21 source scenarios compare detailed/summary payloads, refreshed records, role/visibility decisions and stored-asset file slices. Missing-target preview catches only file/path failures; invalid frontmatter still propagates. Asset reads enforce a separate read grant for the stored concept path, source-specific range messages, manifest/archive validation and full archive SHA before bounded file retrieval. Metadata-only reads intentionally expose stored metadata without validating bytes. Tests cover malformed rows, SQL/refresh/history/archival failures, oversized archives and public Git-backed reads. Error-envelope translation (notably Python KeyError quoting) and broader manifest coercion remain open. Regeneration also fixes previously unfrozen existing-asset timestamps in the submission oracle.
* Selected-change revision: 25 Python cases compare full proposal/asset/event rows and results. Indexes are sorted/deduplicated and checked for bounds/conflicts; archival subsets are rejected; every matching body change must accompany its asset (and vice versa). Copied assets retain identity/bytes/manifest, while the new proposal is curator-owned with fresh timestamps/default text and source metadata. Original review fields and assets remain intact except for prior queue refresh. Tests cover insert/asset/deferred-COMMIT rollback, missing assets, permission/tag checks, post-commit preview/payload failure and public Git-backed calls. No idempotency key is added. Selected indexes are typed Go integers; Python-only coercion at the endpoint remains separate.
* Proposal lists/cursors: 17 Python scenarios compare pages, decrypted cursor scopes, refreshed records and events. Hidden candidates consume page slots; cursor progression uses the last raw candidate rather than the last visible result. Lists require proposer role and write-visible proposals; non-curators are author-filtered. Refresh precedes status/limit/cursor validation. Pure-Go Fernet matches four fixed encryption vectors and eleven Python decode cases; tests reject MAC tampering, invalid padding/block alignment, wrong instance keys and every scope change. Random/key/SQL/render failures, concurrent key initialisation, restart invalidation and 10,000 cursor fuzz executions are covered. List token JSON is sorted rather than insertion-ordered; keys initialise lazily rather than at service construction. Authenticated malformed cursor bodies and non-string after coercion are stricter than Python, and neither implementation imposes a cursor TTL. Endpoint input-size limits remain required.
* Derived content kernel: 20 Python lifecycle steps (models disabled, embeddings deferred/on-path maintenance) and four migration fixtures compare concepts, FTS rows, links, graph metrics, state and retained embedding rows. SQL schema comparison ignores whitespace only; SQLite wall-clock state timestamps are format-checked while concept timestamps remain exact. Two-pass move updates, duplicate IDs, self/external/relative/broken links, revision stamping and per-entry inner commits match source snapshots, including partial updates before a later parse failure. A real SQLite driver wrapper injects failure at each SQL operation, including COMMIT/BEGIN; malformed rows, filesystem errors, unsafe paths and short-lived database handles are tested. Pure-Go proposal publication updates FTS and then submits/reviews/applies archive operations against the resulting index. Models-off code retains/stales existing embedding metadata but does not run inference. Automatic quarantine, inode-based migration caching and search/graph/status endpoints remain separate work; migration is explicit and unsafe incremental symlinks/traversal fail closed.
* Eventual lexical search: 33 query transformations and 56 FTS5 policy/filter/page cases compare Python, including exact observed BM25 scores, snippets, ties, tag/type/status filters, escaped path prefixes and protected/trash exclusion in SQL. Limits clamp to 1-100 and candidate filtering precedes ranking; direct lexical LIMIT/OFFSET is equivalent to the source's larger lexical candidate fetch followed by slicing. Plain query terms retain case/order, drop stop words only when significant terms exist, and match Python Unicode word categories for the captured cases. Tests inject every SQL failure, malformed result rows and query diagnostics, verify no protected paths leak during concurrent content updates, and run 10,000 executions each against query generation and real SQLite MATCH. Unicode database-version edges and Python arbitrary-size/Unicode-decimal/underscore cursor coercion remain gaps; offsets outside signed machine range fail explicitly. Strict freshness, semantic fallback and automatic corruption recovery are not implemented by this kernel.
* Graph/status/freshness: 100 Python graph combinations (four policies, five centres, five depths), four visible-count snapshots, five raw metrics and five wait cases compare exact payloads. Traversal filters each hop before expanding, deduplicates visits and sorts depth/path/ID; broken counts are distinct readable targets, while raw metrics retain duplicate counts. Readable trash centres remain valid although SQL neighbour filters exclude trash. Freshness checks revision equality before deadline/status and polls every 20 ms; consuming reads reject quarantine separately. SQL fault injection covers every graph query, malformed row tests cover close/scan/iteration errors, and concurrent advancement/cancellation tests cover waits. Raw Metrics remains unscoped and must not be exposed without service authorisation. Initialisation and automatic quarantine remain lifecycle-owner responsibilities; Go time.Duration limits differ from Python float timeout extremes.
* Derived lifecycle: 14 Python scenarios compare final state/quarantine filenames for empty/missing/invalid files, unsupported versus cached schemas, inode replacement, missing tables, parse failures, quarantine status, FTS syntax errors and SQLite contention. Invalid write headers fail outside the quarantine catch, while database errors on reads can quarantine. Existing handles close before same-directory rename; replacement state is committed with the quarantined path, and explicit rebuild retains the previous file/path for inspection. Fault tests cover every file/connection/initialisation/recovery branch, sidecar removal, random DB errors and quarantine collision rejection. Reopened recovery-file contents are verified, and proposal/archival integration uses the lifecycle owner. Strict waits release the owner lock between reads. Go serialises each owner, refuses to overwrite a quarantine collision and does not reproduce cross-filesystem shutil fallback or optional native-extension loading. Multiple owners/processes, engine-specific diagnostic wording and power-loss/WAL behaviour need broader tests.
* Trusted identity: 28 Python authentication/context cases and 15 request-authorisation decisions preserve exact lowercase-header/case-sensitive Bearer handling, untrimmed tokens, configured principal-name lookup, managed credential precedence and managed-policy override. Context copies only the authenticated name's resolved policy and transport session ID; argument/header attempts to spoof identity/client/chat are ignored. Tests cover duplicate names/tokens, defensive configuration copies, managed failures without fallback, concurrent principal isolation and a real HTTP session with live grant changes, cross-principal session rejection, admin-tool gating and credential revocation. Empty versus absent principal/session fields and full config coercion remain typed-boundary gaps. This adapter does not register a complete Memento tool surface.
* Service envelopes: 23 exception-family cases, nine Python KeyError repr inputs and four success-default cases compare exact fields. Explicit empty revisions, nullable IDs, empty arrays, warnings and next-tool hints are preserved; repository lookup occurs only for an omitted revision. Unexpected I/O/runtime/corruption/timeout errors propagate rather than becoming validation envelopes. MCP conversion preserves nested typed data and integers above float64 precision through ordered JSON values; unsupported/nonfinite Go values fail. A test-only real HTTP submission adapter verifies trusted author identity, structured/text success, permission-denied service envelopes and redacted unexpected JSON-RPC failures. Full production tool registration is not implied; arbitrary Python error diagnostics/Unicode repr and constructor-coercion equivalence remain gaps.
* MCP worker admission: nine Python cases pin busy/indeterminate envelopes, ordinary-call admission, the second reconciliation slot, exceptions, shielded cancellation and shutdown drain. Normal calls hold the execute-admission flag until completion; two active jobs reject even reconciliation. Wait timeout is 30 seconds in normal use (short injected timers in tests), and cancellation/timeouts leave the admitted job running with context identity retained. Tests verify concurrent saturation, post-cancellation completion, permanent close, cancelled drain, panic cleanup and repeated race runs. This helper requires per-job database ownership from its caller; execute plans and model-worker orchestration remain unported.
* Operation reconciliation: 24 Python cases compare response data, revision selection and errors for all stored states, principal ownership, proposer-only rebase access, control records and changed-path ACL filtering. A missing key probes the writer lock without blocking, then repeats the lookup under an acquired lock to avoid a false safe-to-retry result. Failed rows become indeterminate when repository history advanced; error text truncates at 500 code points. SQL/replay/lock/repository errors are covered. A real apply is held after publication until its worker wait times out; reconciliation returns in-progress, then committed after release, and the original key replays without a duplicate mutation. Python non-string replay proposal coercion and malformed JSON diagnostic text remain gaps; Go's non-reentrant lock cannot emulate same-thread Python RLock probes.
* Registered proposal tools/jobs: nine source-derived definitions and 15 actual Python wrapper calls pin signature defaults, generic uMCP scalar coercion, discovery order and unknown/missing argument errors. Runtime metadata is generated only by the verified source exporter. Registered HTTP tests run submit/get/list/asset-get/revise/rebase/review/apply/operation-get through trusted identity, per-job DB handles, shared cursor keys and session-scoped apply notifications. Managed grants are re-read after acquiring the writer lock; a grant change while queued cannot reuse stale policy. Handles close on all failures, and failed post-commit notification delivery propagates without claiming rollback. MCP text JSON is compared as JSON values rather than dict insertion order; malformed dynamic types, arbitrary-size service integers, full configured surfaces/catalog/resources and complete argument diagnostics remain gaps. Registry is explicitly a nine-tool development subset. CI 35325423801 exposed a test that reused a 1 ms apply timeout for reconciliation; reconciliation now gets a normal deadline, cleanup is failure-safe, and 100 race repetitions pass. Existing reconciliation-oracle setup timestamps are now frozen for repeatable generation.
* Models-off read endpoints: 78 service cases and nine malformed-file cases compare exact envelopes for read-by-ID/path, list prefixes, search modes/cursors, and graph centres. Namespace policy applies without an extra reader-role gate; ID lookup scans only readable concepts, while list parses all readable files before literal-prefix/trash filtering. Malformed hidden files do not leak; malformed readable siblings can block ID/list requests as in Python. Frontmatter preserves microseconds/default fields, graph missing/forbidden IDs share not-found behaviour, and lexical fallback for explicit semantic/hybrid modes retains the source warning. Index/repository revisions can differ without setting index_stale. A 13-tool development registry passes 20 wrapper cases and live HTTP tests, including reads while a writer holds the lock. Zero statement gaps and ten focused race repetitions pass. Enabled semantic inference, configured surfaces/catalog/resources, broader timestamp/coercion/error fidelity and daemon wiring remain open.
* Staging HTTP: 19 actual source route/store cases pin routing, media/auth ordering, owner-scoped reads, idempotent uploads, one-time ticket replay, expiry and public response fields. /assets/staging/upload authenticates with its upload ticket, not a bearer, and media/size checks precede ticket lookup; all other matching routes require proposer bearer before path/method errors. Percent escapes remain undecoded and query strings do not change routing. Responses retain no-store/nosniff headers. Tests add both oversize branches, URL/auth/DB/JSON failures and auxiliary-route integration proving the transport does not pre-authenticate tickets. Response JSON key order is sorted rather than Python insertion order; malformed JSON diagnostics and full daemon upload bounds remain open. Accepted-asset endpoints remain separate work.
* Staging MCP tools: 18 exact envelopes across 21 Python lifecycle steps cover principal-role denials, unavailable stores, issuance, duplicate/conflicting keys, owner isolation and pending/uploaded/expired states. Source-derived metadata extends the explicit development registry to 15 tools and 27 dispatch fixtures. Direct wrappers bypass worker admission and the repository writer lock; a separate mutex preserves Python's non-interleaving direct calls. Tests cover sixteen concurrent begins with one ticket issued, live managed-role changes, configured-principal precedence, spoofed arguments, malformed stored state and redacted DB errors. Registered HTTP runs ticket creation, bearer-free ZIP upload, status and proposal consumption, retaining the uploaded ticket state after the staged asset is consumed. Thirty focused race repetitions pass. This does not establish configured-surface, multi-process or full daemon parity.
* Asset packs/staging: 52 ZIP cases compare stored/deflate/bzip2 data, archive-byte SHA, central-directory order, normalized paths, duplicate/mode/encryption/magic rejection and encoded/file/count/ratio limits. MIME types are pinned to the oracle table and its source hash; SemVer retains Python Unicode-decimal suffix behaviour. Twenty-eight staging/ticket steps compare deterministic IDs/tokens, persisted manifests, ownership, replay after consumption/expiry, and blob clearing. Tests add partial consume rollback, deferred-commit failure, expiration side effects, random/SQL/manifest failures and single-winner concurrent consumption. ZIP-LZMA, malformed archive error strings, full Pydantic manifest coercion, timestamp parsing outside generated RFC3339, nested caller transaction side effects and HTTP wiring remain open.
* Bounded asset retrieval compares 63 range/path reads, nine invalid manifests and 12 malformed ZIP cases. Every returned slice follows a full-file size/SHA-256 check; binary or split-UTF-8 slices use base64. Manifest hash bytes, limits, archive membership/mode checks and stored/deflate/bzip2 CRC checks are tested. An explicit CRC32 comparison closes the standard Go reader's zero-CRC bypass. Accepted metadata/version helpers now have their own fixtures; HTTP upload/read authorisation is still unwired. CI 35307329314 failed the raw statement gate for a flaky HTTP disconnect branch; handshake-triggered cancellation/disconnect tests now cover both select exits deterministically (100 ordinary/race repetitions), verified by native amd64/ARM64 CI run 35307821377.
* Accepted asset repository: three exact metadata/ZIP writes (optional UTC fractional timestamps), seven path-validation cases, four version selections and five retention cases compare Python. Tests cover size bounds, missing/invalid metadata, directories/files/symlinks, read/write failures and immutable-version rejection. Reads intentionally do not validate metadata identity or manifest/digest beyond the source envelope; that remains a service responsibility. Go rejects symlink writes and uses O_EXCL/rooted paths instead of reproducing Python's unsafe direct writer. Metadata-before-ZIP partial failure is retained for transaction cleanup. Numeric-version ties and broad nonfinite JSON/hostile-parent races need further parity review.
* uMCP JSON-RPC ID/response validation and protocol version/error helpers, including bool-versus-int and integers larger than float64 can preserve.
* Actual local sync/async HTTP server scenarios: 41 requests compare statuses, JSON/text bodies and selected headers, normalising only random session IDs. In-process and real-socket Go tests exercise event streams, keepalive, duplicate-stream rejection, principal/version binding, queue cap, TTL/deletion, hooks and I/O failures. The same scenarios also run through the Go async raw listener. The same scenarios now pass through the synchronous raw listener too; the complete upstream transport regression suite remains open.
* Async raw HTTP parsing compares 132 exact byte streams against the pinned Python parsers. Cases cover request/header line limits, header counts, Latin-1/ASCII decoding, duplicate fields, malformed syntax, Python integer grammar, short bodies, origins and compound error precedence. Sixteen additional HTTP cases distinguish sync/async authentication/media/version ordering. The raw Go listener tests pipelining, HTTP/1.0 keepalive, maximum requests, per-read deadlines, cancellation, real legacy SSE exchanges and flush-error propagation. Normalisation excludes header order and insignificant JSON whitespace; parser fixtures retain original bytes. All raw response-byte details and further timeout/disconnect combinations remain open.
* Synchronous parsing/wire responses each have 218 cases captured from CPython 3.13.14 and the pinned uMCP handlers. They cover HTTP/0.9, versions, duplicate/folded/malformed headers, exactly consumed bytes, route-dependent body reads, HTML error bodies and HEAD suppression. Date/Server headers are omitted from comparisons; JSON whitespace is normalised, including headerless HTTP/0.9 JSON. Other error-body bytes and lengths are exact. Live tests exercise both raw listeners, legacy SSE, Continue before body transmission, short reads and timeout closure. CPython stdlib-version differences require separate review; fixture metadata pins 3.13.14 explicitly.
* Legacy SSE compares 68 live sync/async requests plus endpoint handshakes, selected headers and JSON events. Tests cover principal/session binding, trusted subscription IDs and cleanup, fresh IDs on reconnect, targeted/broadcast notifications, unbounded sync queues, async write acknowledgement, keepalive, failed writes/flushes and session rechecks. Sync and async parser-order differences are explicit. Sync unsupported-method HTML is compared in the separate raw-wire fixtures. Notification fallback has 72 sync/async source fixtures covering nil/empty/selected recipients, nil/empty/nonempty params and disconnected transports. Additional tests cover failed async delivery and concurrent stdout writes. uMCP transport argument/dispatch tests compare 70 sync/async source cases: defaults, conflicts, unknown/incomplete flags, integer coercion and file selection. Live tests cover all six sync/async network modes, announcements, stdio/file execution, notifications and cooperative shutdown. Range-valid CLI inputs use the typed transport constructors; nonsensical HTTP limits/endpoints currently fail earlier than Python, and hostname multi-address binding/platform error text require separate parity work. Memento's operational CLI is unported.
* HTTP Accept quality/wildcard and content-type parsing, singleton headers, all status reason phrases from 99 through 600, and response bounds/injection checks. Runtime-typed Go responses deliberately exclude impossible Python dynamic type combinations; transport parsers must reject them at decoding.
* Tool fixtures: 380 sync/async result-format/schema-subset cases and 40 call/coercion/error cases, with dynamic discovery and annotations. Text embedded inside results preserves dict insertion order and Python number formatting. Native Go structs must be explicitly normalised; nonfinite structured JSON, reflection inference and malformed dynamic schema corner cases remain gaps. Handler panics are contained and remote-redacted. Source coercion is intentionally not strict service validation; Memento's typed validators remain required.
* Completion/config/logging scenarios compare both bases: reference resolution, parameter enums, provider output/total/hasMore validation, prefix/dedup limits, source initialise capabilities and log redaction. Session-targeted notification delivery is still a transport gate.
* Resource/prompt fixtures cover dynamic metadata, pagination, static/template reads, binary and nested content, missing URIs, handler errors/cancellation, subscriptions, prompt defaults/coercion and discovery-versus-call docstring metadata. Notification delivery and transport-authenticated session ownership remain separate gates.
* Request-dispatch fixtures emitted independently by both sync and async uMCP bases with the same overridden test method: malformed input, validation ordering, metadata, integer IDs, notification and cancellation results. These isolate dispatch from method implementation and do not prove discovery/tool execution.
* URL/Origin fixtures compare raw urllib-style components, default paths, exact allowlists/authority, loopback/ports, IPv6/IPvFuture errors, Unicode delimiters and error text. `golang.org/x/text/unicode/norm` supplies CGO-free NFKC; the transport must still enforce wire-size/auth rules.
* File mode compares seven sync/async one-shot cases, including universal newlines and strict UTF-8 decoding. TCP compares 34 live Python captures against real Go sockets: fragmented/final lines, multiple requests, invalid UTF-8 and exact 65536-byte async boundaries. Sync mode uses replacement decoding and a fresh 30-second timeout per blocking I/O call; async mode has strict decoding and no socket timeout. Oversized unread input can close with EOF or TCP reset; comparisons preserve bytes delivered before closure. Additional tests cover concurrent peer contexts, cooperative cancellation, notification sinks, timeout/error paths and line-reader fuzzing. CLI argument/announcement parity is separate.
* Actual sync/async stdio loops driven by byte streams: blank and final lines, notifications, invalid UTF-8 replacement and large requests. Expected responses compare JSON objects, not insignificant whitespace. Caller-owned blocked readers still require caller closure to interrupt; the source loop is sequential.
* Pagination fixtures emitted by both bases: principal/list identity binding, exact opaque cursor bytes, Unicode labels, large offsets/sizes, invalid params and the reference's permissive base64/bool/default-size edge cases. Cursors remain data, not authorisation tokens.
* Progress fixtures emitted by both bases: absent token, message sanitisation/rune bounds, integer/float validation, large integer total/progress comparisons and errors. Concurrency tests cover request-ID/progress-group cancellation, duplicate-ID cleanup and isolated metadata.
* Registry: 34 Memento operations, twenty tool-surface/model-option combinations, concrete argument schemas and the execute plan schema. These are forward references; they are not claimed as implemented discovery.
* Rust vector helpers: finite/length errors, empty/dimension/zero-norm cases, scalar values and AXPY results.
* Embedding framing: Rust-produced successful request/response headers and scalar payloads, short reads, invalid arguments, EOF, write/flush failures, oversized headers and nonfinite output. Test-only `go/oracle/check_worker.py` exercises the unchanged Python client against the Go executable and real public model. Malformed serde diagnostics and duplicate known fields remain explicitly incomplete, as do service worker/deadline/resource integration.
* GTE: Rust-generated tokenizer fixtures across limits/Unicode, synthetic zero/one/two-layer model outputs and checkpoint sequences. The original Go tokenizer's bytewise UNK behaviour differs from Memento's Unicode fix; default Go-port tokenisation preserves the latter.
* `make -C go model-test GTE_MODEL_PATH=/path/to/gte-small.gtemodel` is a required native CI step, not an optional skipped release gate. It verifies the model SHA, exact token IDs and five real-model outputs against Rust. Current explicit exploratory numeric gates are max absolute error <=1e-5, cosine >=0.999999 and unit norm error <=1e-5; observed local max error is <=1.2e-7. These are not bit-identical arithmetic or a complete retrieval corpus and need review before production compatibility is signed off.

Regenerate the real-model reference only deliberately with the digest-pinned file:

```sh
cd go
CARGO_TARGET_DIR=../build/go/oracle-target cargo run --locked --release \
  --manifest-path oracle/rust-gte/Cargo.toml -- real /path/to/gte-small.gtemodel \
  > testdata/parity/gte-real.json
```

The public model is fetched/verified by existing test tooling, never at Go runtime. Synthetic parser tests include every truncated offset, invalid dimensions/UTF-8 and oversize header guards. A complete malformed-error-text matrix and mmap/resource tests remain required.

## End-to-end gates still to build

Run an identical scenario against reference and Go daemons and compare HTTP/JSON/SSE output, accepted Git content, control/index tables, model outputs and final operation state. Inject identical clock/UUID inputs where identity matters. Keep nondeterminism normalisation narrow and reviewed: never remove ACLs, status, revisions, warnings, operation IDs or proposal decisions simply to make a diff pass.

Extend coverage to persistent HTTP sessions, auth-bound retries, notifications/cancellation/progress, malformed requests, byte limits and backpressure. Port all existing proposal/asset/rebase/recovery tests and source regression cases, not only successful routes. Crash testing must kill the process before/after publication and prove original-key reconciliation from disk on restart.

`make -C go model-test` now requires both `GTE_MODEL_PATH` and `NEEDLE_MODEL_PATH`; the Needle fixture validates the 31 tensor shapes/raw bytes/expanded FP32 hashes and all 8192 vocabulary entries from the real NDL1 model. Regenerate with `cargo run --locked --release --manifest-path go/oracle/rust-needle/Cargo.toml -- /path/to/memento-router.ndl`. Add `NEEDLE_TOKENIZER_PATH` for the now-required tokenizer gate: 15 encode/decode inputs and all 8192 single-token decodes are compared with Rust. Always-on tests compare 288 synthetic BPE/Unigram normalisation/byte-fallback cases against sentencepiece-rust 0.1.1. The model test also compares five real scalar generation outputs and checkpoint sequences. `make -C go corpus-test` requires the pinned model/tokenizer and compares 360 complete output strings with freshly generated Rust oracle results from corpus SHA `9ffeb303574fa6bd24718adc42c7a3d8c4632e3cf78685d14886c7b24b2ddca9`. The first local scalar run matched 360/360 in 1408.79 seconds; native amd64/ARM64 CI run 35291886039 subsequently passed the full gate on both architectures. Native amd64/ARM64 CI runs this gate without enabling SIMD. A match is behavioural parity, not a claim that the model's output is correct or authorised for execution; Python service validation still needs porting. Source and port reject malformed files, but their JSON/UTF-8 diagnostic wording has not yet been made fully identical.

GTE/Needle gates require exact token IDs, model-file validation, layer/intermediate golden tests, finite/shape checks, model outputs and full routing/retrieval corpora. Floating-point tolerances must be explicit and agreed; no hidden re-quantisation or embedding-space mixing. SIMD remains disabled in these baseline tests.

The final gate is the entire parity matrix plus conformance, fuzz/race/fault tests, resource limits, browser behaviour and state-preserving migration/rollback in disposable copies. Only a separately authorised deployment can replace production.
