# Compatibility matrix

`partial` means some code/tests exist. `reference captured` is not implementation. There are no complete end-to-end subsystems yet. Record tests and results per row rather than treating an aggregate coverage percentage as feature completion.

| Surface | Reference | Required observable match | Status |
| --- | --- | --- | --- |
| Response envelopes | `src/memento/envelopes.py` | Status, fields, empty arrays versus null, revision/warning/reconciliation fields, validation failures | Constructors plus service success/failure mapping pass 23 exception-family, nine KeyError-repr and four success-default fixtures. Known failures retain envelope fields; unexpected I/O/SQLite/runtime errors propagate to transport redaction. Typed service values convert to uMCP ordered results without integer rounding; synthetic authenticated HTTP submission tested. Full endpoint registration, Python-wide diagnostics/Unicode repr and envelope validation fidelity remain open |
| uMCP shared validation | pinned `umcp_shared.py` | IDs without float rounding, booleans rejected as integers, version negotiation, errors, headers, media/origin validation | Implemented and fixture-tested: IDs/responses/versions, media/content type, singleton headers, response bounds/status phrases, origin/URL splitting and NFKC delimiter checks. Further edge cases remain subject to differential fuzzing; transport integration pending |
| uMCP sync/async public behaviours | pinned `umcp.py`, `aioumcp.py`, upstream tests | Same observable client/server behaviours through idiomatic Go APIs; do not claim Python import/API identity | Partial: shared dispatcher validation/context, notifications, cooperative cancellation and progress match sync/async fixture outputs; discovery and method semantics pending |
| uMCP transports | upstream Streamable HTTP/stdio/TCP/SSE docs/tests | Initialise/notification ordering, persistent session binding, session deletion, authentication on every request, response/error/SSE framing, reconnection, cancellation and progress | Partial: stdio and Streamable HTTP handlers match reference scenarios (41 actual Python HTTP cases); UUID sessions, principal/version binding, GET event streams, TTL/caps, subscriptions and deletion tested. File mode matches seven sync/async cases; TCP matches 34 live reference cases including divergent UTF-8/line limits, plus per-I/O timeouts and cooperative shutdown tests. Legacy SSE matches 68 live request/event cases across sync/async modes, with principal binding, notifications, subscription cleanup, reconnect and I/O failure tests. Async raw HTTP parsers/listener match 132 parser cases, 16 auth-order cases and the existing 41 HTTP scenarios; pipeline, limits, cancellation and flush errors tested. Notification routing/fallback matches 72 source cases; stdout writes are serialised and configurable. Synchronous parser and wire fixtures cover 218 cases each against Python 3.13.14, including HTML errors, route-dependent reads, Continue and legacy SSE. The 41 HTTP scenarios pass through both raw listeners. uMCP transport args/dispatch match 70 cases with all six live network combinations. The five mutable Streamable HTTP server settings now drive session expiry/limits, keepalive, read timeout and per-connection request caps in both parser modes; invalid settings fail before binding. Asynchronous hostname binding resolves every distinct address, opens and announces each socket (including independent ephemeral ports), rolls back partial binds and drains listener loops together. Synchronous mode retains one stdlib listener. Memento operational CLI, Python's late dynamic-type failures for mutated settings, The Go-port CI now runs the Python service, including its pinned uMCP dependency, under CPython 3.12, 3.13 and 3.14; stdlib-sensitive raw-wire fixtures remain explicitly pinned to 3.13.14. Broader upstream uMCP and cancellation/session conformance remain pending |
| Tool/resource/prompt discovery | registry/server + upstream tests | All tool surfaces, auth-sensitive discovery, optional route/answer, resource templates and dynamic prompts, bounded arguments and returns | Partial: dynamic tool registry/discovery/calls, schema subset, result formatting and principal/list-bound pagination match sync/async fixtures. Dynamic resource/template/prompt registration/list/read/get, binary/JSON content and subscriptions also match fixtures. Completion enums/providers/caps, logging/redaction and initialise/config composition now match scenarios. Nine implemented proposal/control tools now have explicit registration with source-derived metadata/order and 15 wrapper-call fixtures. Registered HTTP tests cover submission, listing, asset reads, revise/rebase/review/apply/reconciliation and session status notifications. Read/list/search/graph add a 13-tool subset with 20 total wrapper fixtures, 78 service cases and nine malformed-file cases. Models-off explicit semantic/hybrid requests retain lexical fallback warnings; read APIs are not writer-locked, matching Python. Staging begin/status, accepted-asset reads/pruning and direct create/patch/rename/trash/restore/purge plus namespace inventory/manifest comparison/asset metadata plus real models-off help/status and writable-scope audit expand the subset to 29 tools with 75 dispatch fixtures; direct staging wrappers preserve principal-role checks and bypass worker/writer admission. Accepted-asset reads require reader policy and take their own repository lock after range validation. Catalog/help payloads now match all twenty configurations with 34 contracts/five workflows; guarded registration, resources/templates, completion and asset-publication prompt match 15 protocol cases. Hidden contracts/tools remain callable as in Python; callbacks own authorisation. Configured registration requires all operation handlers, so missing services/execute cannot be advertised through a partial surface. Managed access-tool discovery, daemon composition, remaining handlers, complete dynamic argument coercion and malformed diagnostics remain unfinished |
| Direct curator mutations | `service.py` create/patch/rename and `_commit_changes` | Validation/catch ordering, warnings, exact request hashes, authorised replay, postpublication refresh/preview and notifications | Create/patch/rename match 20 scenarios/21 steps and real Git/HTTP tests. Model failures propagate to uMCP; service errors use envelopes. Body edits to asset-backed concepts fail before replay; request hashes omit expected revision. Postpublication queue/tracking/preview failures preserve succeeded operation state. Eight concurrent same-key creates yield one publication. Trash/restore/purge add 31 scenarios/35 steps: exact move bytes, unchanged inbound links/assets, strict confirmation before policy, both source/destination grants, asset-first purge and request hashes including expected revision. Registered HTTP verifies historical files after purge, notifications and replay; prepublication rollback and concurrent purge pass. Broader Pydantic diagnostics, hostile external filesystem replacement and enabled hot-memory integration remain open |
| Inventory/manifest/asset metadata | `service.py` inventory, compare_manifest, asset_metadata | Prefix permissions, projection, cursor boundaries, bounded comparison and metadata summaries | Inventory matches 146 cases with directory pruning, partial-grant intersections, explicit trash traversal, field deduplication, lexical cursors and selected-page parsing. Assets are read only when requested and require a string digest, not archive verification. Registered HTTP bypasses writer lock; concurrent projections are isolated. Manifest comparison matches 52 scenarios, 61 timestamp fixtures and seven asset summaries: input/remote/combined limits, templates/aliases, target safety, hash-driven grouping, timestamp ordering and second live-policy resolution. Arbitrary-size byte counts remain JSON integers. HTTP/concurrent/fuzz tests pass. Asset metadata matches 68 service cases and seven aggregate-budget scenarios: trash-inclusive prefix scans, latest/detail selection, bounded sorted file lists, strict stored fields, skill-body hashes and unverified archive sizes. Git author-date fallback matches linear add/modify/delete/re-add history. Full Python dict/Sequence/Pydantic coercion, merge-history/pathspec timestamp fidelity and exotic ISO diagnostics remain open |
| Models-off service help/status | `service.py` help/status + direct server wrappers | Policy error boundaries, worker bypass, writer lock, scoped counts, proposal refresh/backlog, revision defaults, disabled features/readiness | 36 source scenarios compare exact envelopes plus proposal rows/events. Ready counts use index scope; unready/missing-revision fallback scans readable concepts including trash. Help policy errors propagate; status catches mapped failures. Real resources and registered HTTP bypass worker saturation; status still waits for writers. Enabled-model readiness, full runtime config and empty-read-prefix SQL-error fidelity remain open |
| Service audit | `service.py` audit, writable scope, graph adapter/cursor helpers | Read/write intersections, repository issues, optional graph availability, filtered pages, repair guidance and cursor binding | 560 source cases, eight scope fixtures and 19 cursor cases compare the service layer. Repository issues filter by path only; graph severity/rule/limit/cursor apply separately. Unconfigured/unavailable/stale graph paths return before cursor validation. Revision/filter-bound cursors preserve permissive Base64 and numeric version equality; every page obtains a fresh policy-scoped snapshot. HTTP worker admission, writer-lock bypass, changed-scope continuation, failures/races/fuzz pass. Graph snapshot construction and its rules remain unported; malformed JSON/surrogate diagnostics and broader upstream conformance remain open |
| Execute plans | `executor.py` | Type/reference rules, strict resolved values, limits, projections, single-commit rule, partial success and post-commit reconciliation | JSON value kernel matches 140 source cases for eager reference detection, nested resolution, field extraction, recursive list caps, return projection/asset exemptions and progressive post-commit output fitting. Large integer values stay exact; exception families and final-newline regex quirks are retained. Thirty race repetitions and 10,000 fuzz cases pass with no input mutation. Structural plan/limit models and tool-argument normalisation match 173 additional source cases (110 plans, 32 limits, 21 normalisation, ten real executor preflights). Source-derived contracts identify 29 execute-capable operations, required fields and commit flags. Preflight enforces operation/commit caps before static argument validation; referenced operations defer typed checks until strict resolution. Typed validators for 27 operation models match 5,316 Python cases, comparing normalised values, full ordered error lists and bounded diagnostics. The narrow source-derived contracts retain defaults, nullable fields, tuple-item validation, scalar coercion, strict JSON types and metadata scope checks; unsupported constraints fail at construction. Propose/compare_manifest nested validators, dispatch/admission/deadlines, post-commit accounting and registered execute endpoint remain unimplemented. Multi-error object iteration order, Python-only tuple inputs, giant integer conversion limits and malformed runner-owned payload behaviour remain gaps |
| Principal/access administration | access/authz/config/admin modules | Trusted identity, role/namespace grants, deny-before-ranking, credentials, admin profile separation, access revision/audit, HTTP security | Policy helpers pass 630 path cases; managed access store matches 32 lifecycle steps, encrypted verifier bytes/digests, key rotation and DB/audit rows. Trusted identity adapter matches 28 static/managed auth/context cases and 15 request-authorisation decisions; HTTP tests verify live policy changes, no managed-to-static token fallback, session principal binding and revocation on the next request. Service tool registration, complete config models, multi-handle final-admin races and malformed persisted-value coercion remain open |
| Runtime config/CLI | config/cli/app/server | Defaults, environment precedence, error messages, read-only startup validation, graceful drain, signal handling, command exit behaviour | Development status command only; CLI not ported |
| Markdown concept repository | repository modules | Front matter/body preservation, IDs/paths, links, assets, symlink containment, Unicode, rename/restore collisions, trash ACLs | Partial: canonical bundle paths, reserved-name classification and filesystem read/write validation match 37 synthetic cases, plus root/IO failures. Concept metadata/body validation has 353/9 cases; frontmatter parsing/serialization has 145/253 cases. Includes source timestamp/default-enum quirks and YAML quoting. Links/renames match 118 documents plus 12 scheme and six rename edge cases; bundle scan/audit has ten filesystem scenarios and three generated-output cases. Worktree create/patch/rename/trash match 27 source fixtures including partial failures and exact bytes. Copied metadata serialization preserves Python model_copy's validation bypass, tag order/duplicates/empty strings; trash preserves bytes and rename prechecks all rewrite grants. Transaction integration verifies publication, replay and failed-write isolation. Full Markdown/URL and YAML graph/key/type/resource parity, casefold-tie ordering, replacement races and proposal apply orchestration unfinished |
| Git object/ref/recovery semantics | repository Git/transactions modules | Canonical revision, tree layout, atomic ref CAS, lock/recovery behaviour, commit attribution, replay and rollback across crashes | Writer lease ported: six owner/contender fixtures plus cross-process crash/reacquire and error cleanup tests. Packed/loose SHA-1 object/ref reads, tree/blob reads and no-rename diffs ported using go-git. Main CAS uses Git-compatible exclusive .lock/rename, tested against native update-ref, with post-rename durability errors explicit. Exact-revision plain checkout materialization matches contents/executable/link fixtures, using os.Root containment with error/collision tests. Blob/tree/commit encoding reproduces reference hashes. Native-compatible detached linked worktrees and literal exact-path commits pass native Git status/fsck and leave main unchanged. Bootstrap reproduces seeded root commit and supports empty repositories/current checkout. Timestamps, repeat-bootstrap behaviour, external seed symlinks, ignore/pathspec/attributes/filter semantics, reflogs/symbolic refs and durability of new objects unfinished. Transaction apply/replay/startup recovery matches nine checkpoint fixtures and pre/post-publication process-kill tests |
| Control SQLite | control modules/schema 10 | Existing file/schema compatibility, migrations, proposals/history/assets, operations/idempotency, checkpoints and audit | modernc.org/sqlite v1.38.2 selected; WAL/FK/busy-timeout, schema v10, fourteen migration scenarios (including v5 data lift/rollback) and seventeen operation transitions match Python DB fixtures. Exact raw-request hashes and sorted ASCII result/patch JSON tested. Transaction apply/replay/startup recovery wired to Git and verified at nine checkpoints plus process kills. Managed access and low-level proposal/attached-asset records ported; nine status/asset fixtures and 45 queries match. Failed COMMIT explicitly rolls back on a pinned connection. Staging/ticket stores match 28 lifecycle steps, consumed/expired blob clearing and failed-commit rollback. SQLite version/edge JSON, recursive callback/replay coercion and power-loss parity unfinished |
| Proposal lifecycle | service/control tests | All statuses, visibility, conflict classification, reject/request_changes at old base, same-ID proposer rebase, stored asset/author preservation, concurrent apply/rebase/review | Low-level create/get/list/blind-status-update and attached-asset persistence ported. Conflict classification and bounded queue refresh match 63 reference status/scenario combinations; stale and expired transitions append history atomically without clearing review/author/asset state. Missing historical bases fail closed after checking live main. Non-string path coercion and malformed-patch diagnostics differ; caller must serialise refresh with mutations. Change input/defaults, archival-batch policy and author/curator visibility checks match 181/483/350 reference cases. JSON-domain model validation preserves null/list/status semantics, but Pydantic diagnostics and non-JSON Python inputs differ. Internal same-ID rebase matches 27 source scenarios, retaining assets/author/expiry, requiring clean conflicts and skill tags, and atomically journalling review-field clearing plus replay state. Refresh commits before later rebase rejection, matching Python. Rebase shares the repository lock across handles; the daemon still needs its writer lease. Bounded summaries and asset/body matching pass 24 reference cases, including missing content, duplicate matching paths, truncation and optional conflicts. Archival inspection reads an existing derived SQLite index read-only and matches 19 report/error plus 18 visibility cases, including pre-filter reference limits and reviewer-scoped recomputation. Malformed stored-report type handling is stricter than Python's truthy recompute shortcut. Internal review passes 56 Python scenarios across approve/reject/request_changes/invalid decisions; review status, summary and operation journal commit atomically, with optional generated keys and auth-before-replay. Full proposal payload/history and previews match 11 preview, six difflib and three payload fixtures, retaining a 50-event descending history cap. Internal apply matches 18 policy/state fixtures using a synthetic publisher, plus real Git publication, archival, concurrent retry and reopened replay tests. Proposal status/queue refresh follow publication; operation-success/proposal-not-applied split states intentionally persist as in Python. Non-model submission/preparation passes 33 source scenarios plus 99 strict-base64 cases. Inline/staged ZIP validation, ownership/kind/version, canonical skill bodies/tags, active-version exclusion and atomic proposal/asset/consume writes are tested through submit-review-apply. List matches 17 scenarios, preserving raw-candidate page slots, write-visible filtering and scope-bound ephemeral Fernet cursors; four token vectors and eleven decode cases match Python. Cursor JSON uses sorted keys, initialisation is lazy, and non-string authenticated cursor-after coercion is stricter. Revise matches 25 scenarios including sorted/deduplicated selections, body/asset pairing, retained originals and copied selected assets; creation commits before preview failures. Get/asset-get match 21 scenarios: proposer role, per-record/per-asset visibility, summary/detailed fallback and whole-archive validation before file slices. Non-string Python str() coercion, missing-key error envelope quoting and exotic manifest JSON escaping remain gaps. Derived index lifecycle/search integration, model-assisted proposals, optional hot-memory tracking, trusted-context endpoint wiring, envelopes and full cross-process service reconciliation remain unported |
| Asset transport/retrieval | staged assets/asset retrieval | Validation, one-time staging tickets, inline limits, ranges/digests, byte-identical pack access, authorisation | ZIP validation matches 52 cases (stored/deflate/bzip2) with size/count/ratio/path/mode/magic checks; staged/ticket store matches 28 lifecycle steps. Bounded retrieval matches 63 reads, nine invalid manifests and 12 malformed ZIP cases, with full-file hash and explicit CRC checks. Accepted metadata bytes, immutable version writes, kinds/versions/latest and retention helpers ported with filesystem rejection tests. Stored-asset worktree application and existing-concept adaptation match 14 cases; aggregate mutations are verified through Git publication/replay. Staging auxiliary HTTP route matches 19 source requests/lifecycle cases with ticket-before-bearer upload handling, proposer auth elsewhere, replay/expiry/size/media checks and security headers. MCP begin/status matches 18 envelopes across 21 source lifecycle steps; registered HTTP verifies ticket issuance/upload/proposal consumption and principal isolation. Concurrent begin/status wrappers are serialised without the repository lock. Accepted-asset retrieval matches 80 source cases: role/range ordering, semantic version selection, identity/digest checks, manifest-only metadata reads and whole-archive verification before bounded archive/file slices. Registered HTTP verifies ticket-to-accepted bytes and writer exclusion. Pruning matches 29 scenarios/30 steps including all stored proposal statuses, write-only ID lookup, no-op before revision/key validation, boolean retention request hashes, ordering, replay and partial unlink failure. HTTP verifies status-only notifications; concurrent calls and prepublication rollback are tested. ZIP-LZMA, full manifest coercion/malformed diagnostics and source unsafe-write equivalence unfinished |
| Derived SQLite/FTS/vector search | derived index/search tests | FTS5 literal/explicit syntax, stable ordering/ranking, vector SQL functions, model-space identity, namespace filtering, rebuild/checkpoint semantics | Models-disabled content kernel ported: schema v2/FTS5, rebuild, two-pass path updates, structural links/metrics and existing embedding-row maintenance match 20 lifecycle steps and four migration fixtures. Inner per-entry commits preserve source partial failures. Every SQL boundary has injected failures; real proposal publication/archival consumes the Go-maintained index. Update paths reject unsafe symlinks/traversal more strictly. Eventual lexical search matches 33 plain/FTS query cases and 56 ranked/filter/page cases with exact observed BM25 scores; namespace/protected/trash filters precede ranking. Concurrent update/read and two 10,000-execution fuzz targets pass. Graph/status/freshness kernels match 100 graph, four scoped-status, five raw-metric and five wait cases. ACL-scoped BFS, distinct broken counts, trash-centre semantics and 20 ms strict waits are covered with per-SQL faults and concurrent advancement. Index lifecycle matches 14 scenarios for inode-cached initialisation, transient contention, invalid headers, quarantine/rebuild and query errors; strict waits release the owner lock between reads. Go closes owned handles before moving files, serialises one owner and refuses quarantine collisions rather than overwriting. Native extension loading remains intentionally absent; semantic refresh, multi-owner/process recovery, SQLite diagnostic fidelity, Python offset/Unicode version edges and resource parity unfinished |
| Scalar vector helpers | `memento-vector` | F32LE bytes, finite/shape errors, dot/cosine/AXPY, both matrix orientations, all tail/empty cases | Partial: encoding/dot/cosine/AXPY; Rust oracle fixtures |
| GTE1 model/parser/tokenizer | `memento-gte`, converter, go-gte attribution | Same file format, dimensions, bounds, vocab/WordPiece/truncation, mmap/lifetime, bad-file rejection and cancellation | Partial: GTE1 copy-loader/tokenizer and malformed-file tests; mmap/resource limits and full parser-error parity remain |
| GTE scalar forward pass | Rust inference, GTE1 assets | Masked attention, layernorm/GELU, mean pooling/L2, single/batch equality, golden vectors and search ranking | Partial: scalar forward pass and cancellation match synthetic Rust fixtures; five real-model inputs have exact token IDs and max absolute vector error below 1.2e-7 locally. Broader retrieval/corpus and worker integration pending |
| Embedding worker protocol | `memento-embed`, Python subprocess client | Exact frame lengths, LE headers/payload, IDs/errors/bounds, short reads/EOF, cancellation/deadlines and output validation | Partial: Go executable handles info/embed/embed_batch and unchanged Python client accepts real-model replies; malformed serde diagnostic/duplicate-key fidelity, host deadline/resource integration pending |
| Needle NDL1 and SentencePiece | `memento-needle`, tokenizer dependency, converter | Section hashes/bounds, BF16 decoding, model configuration, normaliser/segmentation/special tokens and vocab IDs | Partial: NDL1 parser and BF16 validated against all 31 real-model tensors and 8192 vocabulary pieces. Pure-Go SentencePiece BPE/Unigram, normaliser and Needle special tokens now match 288 synthetic oracle cases, 15 real encode/decode cases and 8192 token decodes. Duplicate JSON/error-text fidelity, wider model/corpus and inference remain pending |
| Needle scalar inference/routing | router/Needle tests and 360-case corpus | Encoder/decoder math, cancellation checkpoints, generated tokens, tool allowlist and field/projection expansion, UNKNOWN behaviour | Partial: scalar inference and constrained generation pass five output/checkpoint cases and 360/360 full held-out Rust outputs locally. Native amd64/ARM64 full-corpus CI passed in run 35291886039; Python routing/field/projection expansion and integration pending |
| Worker lifecycle/model transport | model transport/semantic deferred tests | Backpressure, request ownership, nonblocking health, DB contention retries, dead-worker status, shutdown, original-key reconciliation, memory release | MCP memory-call admission/drain matches nine source cases: one ordinary job plus a reconciliation slot, busy/indeterminate envelopes, 30-second shielded waits and non-cancelling timeout/disconnect. Panic cleanup, concurrent cap and drain tested. Operation reconciliation matches 24 source cases and a real timed-out-apply/original-key replay test, including nonblocking writer probes, scoped changed paths and control-record access. Per-job database/control/staging/access handles now close after work and share the cursor key; managed grants are resolved under the serialized service lock. A queued-policy-change test checks stale grants are not used. Execute-plan admission, model workers and daemon shutdown/telemetry integration remain open |
| Graph/admin/web UI | graph/admin modules and browser tests | Endpoint shape/security/visibility/layout, worker status, unchanged static UI behaviour and browser suite | Not ported; reuse current frontend assets |
| Backups/import/export/repair | backup/CLI/skill import tests | Byte-preserving accepted state, retention, restoration safety, deterministic failure and crash recovery | Not ported |
| Packaging/operations | release/container/model smoke tests | CGO-free binary, no Python/Rust/Git runtime dependency, non-root/read-only config, original mounts/models, architecture builds and signal/resource limits | Scaffold cross-build only |
| Native C ABI compatibility | `memento-ffi`, `memento-needle-ffi`, SQLite extension | Inventory all consumers. C-shared compatibility conflicts with the final CGO-free runtime; resolve deliberately, never silently delete a used API | Open architecture gate |
| In-flight Vulkan branch | separate `feat/gte-vulkan-pretest` | Track accepted upstream behaviour without mixing SIMD/GPU optimisation into scalar phase | Deferred; baseline does not include it |

## Matching rules

Wire semantics and persistence state transitions must match exactly. JSON member ordering is ignored where the protocol treats objects as unordered; no general normaliser may hide missing fields, null/empty differences, different errors or changed decisions. Time, UUID and commit-clock inputs need controllable fixture injection, not blanket removal from outputs.

Float32 calculations can differ when the source uses hardware FMA/reduction ordering. Test scalar mathematical steps and committed golden outputs, then model-level error bounds and exact token/ranking outputs. Any numerical tolerance must be explicit, justified and separately accepted; passing a loose cosine threshold is not 1:1 correctness. The current tiny vector fixtures allow absolute error at most 1e-6 and do not establish model parity.

Crash/timeout, concurrency and security cases are required for completion, even if ordinary unit tests already execute every statement. A port cannot declare success while the matrix contains missing or merely captured surfaces.
