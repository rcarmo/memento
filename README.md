# memento

A minimal Python project scaffold. Its purpose and public API will be filled in from the project briefing.

## Development

```bash
make install
make check
```

The package uses a `src/` layout and supports Python 3.10 or newer.
