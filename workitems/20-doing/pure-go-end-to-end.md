---
id: pure-go-end-to-end
title: End-to-end pure Go port with scalar correctness and full coverage
status: doing
priority: high
created: 2026-09-17
updated: 2026-09-18
estimate: XL
risk: high
tags: [work-item, port, go, parity]
owner: pi
---

# End-to-end pure Go port

## Summary

Port Memento's entire service and inference runtime to Go, including uMCP wire/transport behaviour, storage and recovery, GTE and Needle. Preserve one-to-one behaviour before introducing SIMD or other performance changes. The user explicitly requires full test coverage and permits uMCP Git tip as a reference.

## Acceptance Criteria

* Isolated `go` branch/worktree; no changes to the active Vulkan checkout or production.
* Runtime builds with `CGO_ENABLED=0` and needs no Python/Rust/C inference, native SQLite extension or external Git executable for core operations.
* All rows of [the parity matrix](../../docs/go-port/parity.md) complete with actual reference/Go comparison tests; no placeholder success handlers.
* Zero uncovered statements in every implemented Go package, no exclusions, plus explicit edge/error/race/fuzz/crash tests and all upstream conformance cases.
* Preserve model/file/wire formats, trusted identity and ACLs, atomic mutation/idempotency, proposal/rebase/history/assets, bounded memory and shutdown semantics.
* Scalar numerical and tokenizer/output parity first; SIMD/GPU/quantisation deferred. Any floating tolerance or ABI exception is explicit and reviewed.

## Implementation Paths

A (chosen): side-by-side pure Go subtree with pinned oracles, slice-by-slice tests and full replacement gates. Keeps the existing runtime available for differential tests and rollback.

B (rejected for this request): replace only the Rust workers and retain the Python service. Lower risk, but does not satisfy the user's end-to-end requirement. CGo/native-inference wrappers also do not satisfy the pure-Go runtime target.

## Test Plan

* `make -C go check`: format/vet, strict statement coverage, CGO-free tests/build.
* `make -C go race`, `make -C go fuzz`, `make -C go cross`; native amd64 and ARM64 CI.
* Independent pinned Python/uMCP/Rust oracle generation, checked-in synthetic expected outputs.
* Full uMCP sessions/transports/auth conformance; service/security/storage/concurrency/crash suites; GTE/Needle model/tokenizer/routing goldens and browser tests as each subsystem lands.
* No production fixture mutations. Migration and rollback on disposable state copies before any separately approved deployment.

## Definition of Done

* [x] Dedicated branch and reference revisions recorded.
* [x] Scope/index, staged plan and test policy written.
* [x] Initial dependency-free Go code with oracle-backed tests and strict coverage gate.
* [x] Native amd64/ARM64 CI passed for the initial slice.
* [ ] All uMCP and service surfaces ported and tested.
* [ ] Pure-Go persistence/Git/SentencePiece decisions proven by experiments.
* [ ] Scalar GTE and Needle parity complete.
* [ ] Full operational/resource/crash/migration/rollback gates passed.
* [ ] External C ABI consumers resolved without silently weakening scope.
* [ ] User-authorised deployment and complete validation before production replacement.

## Updates

### 2026-09-18

* Models-off server assembly now registers the verified 29-tool development surface plus memory_execute on a fresh uMCP server. Setup completes before mutation; protocol tests verify discovery and execution, with nil/bad/incomplete-definition guards. Twenty races and zero-uncovered service checks pass. Configured 34-tool/admin/model composition and daemon transport lifecycle remain.
* memory_execute endpoint composition/registration now uses prebuilt execute/raw templates, captures trusted identity before shielded admission, runs the 29-operation dispatcher and converts final service envelopes. Registration adds only the generated execute definition. Real protocol tests cover invalid/empty/read plans, identity, operation caps and revision failures; twenty races and zero-uncovered service checks pass. Daemon/full configured composition remains next.
* RawExecuteTemplate now validates/snapshots the operation allowlist once and binds only principal/session per admitted request, avoiding endpoint map rebuilding. Duplicate/empty/missing operations, invalid composition/identity, sixteen binds and twenty race repetitions pass with zero uncovered service statements.
* Added immutable execute.Factory: embedded planner/argument contracts construct once and are shared read-only by per-request runners; dispatch and clock remain request-local and injectable. Constructor failures, thirty races and 10,000 fuzz cases pass with zero uncovered statements. This clears the endpoint composition API mismatch.
* Raw nested execute dispatch now accepts captured principal/session values and an exact operation allowlist, uses fresh per-operation DB/policy/locking state without worker re-entry, and converts through the ordered envelope adapter. Constructor/unknown/duplicate guards, real read, purge policy branches and twenty races pass; service has zero uncovered statements. Endpoint composition/registration remains next.
* `Workers.Execute` now owns outer plan admission. One shielded plan blocks ordinary calls, permits memory_operation_get reconciliation, returns indeterminate on timeout while detached work continues and clears admission only after completion. Cancellation/nil-work and thirty focused race repetitions pass; service retains zero uncovered statements. Raw nested callbacks and registration remain next.
* Execute's service adapter now snapshots the 29 catalog callbacks, dispatches `memory_<op>` under the existing trusted context and converts ordered uMCP envelopes into runner results. Nested ordered objects/arrays and arbitrary-size numbers are retained; missing handlers, malformed envelopes and nonfinite values fail closed. Service has zero uncovered statements. Worker admission and registration remain next.
* The service-independent execute runner now matches all eleven deterministic source scenarios: sequencing, references, stop/continue, revisions, commit classification, deadlines, failed saves, output fitting and post-commit recovery. Direct branch tests cover constructor/preflight/save/reference/dispatch/limit failures. Execute has zero uncovered statements; thirty races and 10,000 argument fuzz cases pass. Service adaptation, worker admission and endpoint registration remain next.
* Captured eleven deterministic MemoryExecutor loop scenarios with a typed synthetic service and patched monotonic clock: normal sequencing, saved references, stop/continue, commits, deadlines before/after commit, failed-save return skipping, post-commit projection recovery and output fitting. Ruff/mypy and a Go corpus integrity test pass. This is runner reference capture, not implementation.
* The final executor argument model, propose, now passes 322 strict/lax source cases across its five non-discriminated branches. Asset transport exclusivity, exact branch/non-extra issue order and strict saved-reference resolution pass. Extra issue placement/order uses the documented lexical map boundary. Execute has zero uncovered statements; thirty races and 10,000 argument-fuzz executions pass. Runner/admission/deadlines are next.
* Execute compare_manifest argument validation now matches all 312 source cases: nested items/match dictionaries, bounds/defaults, alias caps, after-model mapping requirements, strict/lax datetime coercion and complete ordered diagnostics. Strict validation after saved-reference resolution also passes. Execute/pydatetime have zero uncovered statements; thirty races and 10,000 argument-fuzz executions pass. Only the propose union remains among operation argument models.
* The executor datetime parser now structurally reproduces every applicable pydantic-core diagnostic in the 312-case manifest corpus, including strict separator/range/timezone reasons and lax date-or-datetime fallback text. It does not use fixture-value lookup; permissive service parsing is unchanged. All local gates, thirty focused races and zero-uncovered checks pass.
* Neutral datetime errors now carry a structured parser reason while preserving broad `errors.Is` categories. Existing service envelopes remain unchanged and both packages retain zero uncovered statements after all local gates. This is the error-contract prerequisite for the 18 compare_manifest Pydantic diagnostics, not their implementation.
* Captured 312 compare_manifest executor argument cases through Python's actual `_validated_operation`: 156 lax and 156 strict-JSON calls spanning nested fields, bounds, alias/mapping checks and all 61 datetime cases. Ruff/mypy pass and a Go integrity test pins corpus shape. This is reference capture, not nested-validator implementation.
* The first Python compatibility matrix run passed Ruff, mypy and all 442 tests on 3.12/3.14 but failed graph-check because its new job omitted Bun. Added the same pinned Bun 1.3.14 setup used by main CI; remote rerun remains required.
* Neutral datetime parsing now includes executor-only Pydantic JSON coercion: strict mode accepts strings, lax mode also accepts JSON numbers, magnitudes beyond ±20,000,000,000 use milliseconds and fractions round to microseconds. Focused tests retain zero uncovered statements. This leaves service parsing unchanged and clears the last prerequisite for compare_manifest argument validation.
* Python-compatible aware datetime parsing now lives in neutral `go/internal/pydatetime`, avoiding an execute->service cycle when nested argument validation lands. Typed invalid/naive/overflow categories map back to the same service envelopes. All 61 source cases, thirty helper race repetitions and 10,000 fuzz executions pass with zero uncovered statements. The normal repository race gate will cover service; a combined ten-repeat service run exceeded its 150-second wrapper on the final repetition without a race or assertion report.
* The Go-port workflow now checks out pinned uMCP `30cce7dfe08c6ee63de235f7d81754ba286dafbb`, runs Ruff and all 254 upstream tests, and records branch coverage separately from Go differential tests. The local CPython 3.13 suite passes with 60% combined branch coverage (66% async, 55% sync). Upstream has no coverage threshold; remaining uncovered behaviours still require port-specific fixtures.
* Go-port CI now runs Python service/uMCP compatibility on CPython 3.12, 3.13 and 3.14, covering lint, formatting, typing, the full service test suite and graph checks. Stdlib-sensitive wire fixtures stay pinned to CPython 3.13.14. The local 3.13.14 leg passes 442 tests with two skips; all matrix legs require remote CI. The complete upstream uMCP suite remains separate.
* Asynchronous hostname multi-bind now resolves all distinct addresses, opens and announces each listener, preserves independent ephemeral ports, rolls back partial binds and drains siblings after failure/cancellation. Literal addresses and synchronous transports retain one listener. Focused uMCP tests have zero uncovered statements and ten race repetitions pass.
* Streamable HTTP server settings now match Python's five mutable defaults and feed both sync and async listeners: session TTL, keepalive, request timeout, session cap and requests-per-connection. Invalid typed settings fail before socket creation. The uMCP package has zero uncovered statements; twenty focused race repetitions and 10,000 transport-argument fuzz executions pass. Python's arbitrary dynamic replacement types and hostname multi-bind remain documented gaps.
* Execute argument validation now covers 27 models and 5,316 Python cases, including full ordered issues, defaults, strict resolved references and metadata scope checks. Invalid tuple items are excluded before minimum-length checks, matching Pydantic. Unknown contract constraints and the two unported nested models fail closed. All local Go/reference gates pass with zero uncovered statements; thirty focused race repetitions and 10,000 argument-fuzz executions also pass. Independent review timed out. Nested proposal/manifest models and runner/admission/deadlines remain next; memory_execute stays unregistered.
* Execute structural planner matches 173 source cases: 110 plan models, 32 limits, 21 normalisations and ten preflights captured from the actual executor. Added source-derived contracts for 29 execute operations, operation/commit caps, delayed saved-name checks and strict/lax validator boundaries. Missing validators fail closed; callbacks do not stand in for the unported per-operation models. Forty focused races, sixteen concurrent parses, plan fuzz and all local Go/reference gates pass with zero uncovered statements. Runner/admission/deadlines, operation validators and endpoint registration remain next; bounded source analysis confirmed ordering, while independent code review timed out.
* Execute value kernel matches 140 Python helper cases: reference detection/resolution, field extraction, recursive bounds, asset-aware returns and progressive post-commit output fitting. Preserved large integers, exception families, failed-save shadowing and final-newline regex behaviour. Thirty focused race repetitions, 10,000 fuzz cases and all local Go/reference gates pass with zero uncovered statements. Real tool count remains 29; plan validation/strict resolved arguments, dispatch, admission/deadlines and commit accounting remain next. Delegated helper review timed out.
* Native amd64/ARM64 status/help run [35344212882](https://github.com/rcarmo/memento/actions/runs/35344212882) passed. Audit run 35347296916 remains in progress.
* Service audit adds the 29th real tool with 75 dispatch fixtures. Compared 560 source cases, eight writable-scope fixtures and 19 cursors. Preserved path-only repository issue filtering, scoped graph pagination, early unavailable/stale returns, repair guidance and revision/filter-bound cursors. HTTP retains help/status resources; changed-scope continuation, twelve concurrent audits, twenty-five focused races, cursor fuzz and all local Go/reference gates pass with zero uncovered statements. The real graph snapshot builder/rules, execute and model orchestration remain open; delegated review timed out.
* Native amd64/ARM64 detailed metadata [35340668203](https://github.com/rcarmo/memento/actions/runs/35340668203) and catalog [35342472500](https://github.com/rcarmo/memento/actions/runs/35342472500) runs passed. Status/help run 35344212882 remains in progress.
* Real models-off help/status and resources expand the subset to 28 tools/71 dispatch fixtures. Thirty-six source cases compare index/fallback counts, stale revisions, proposal refresh/backlog and persisted events across four policies. Direct handlers bypass worker saturation; status retains the repository lock, and help/status preserve distinct policy error handling. Eight concurrent snapshots, ten focused race repetitions and all local Go/reference gates pass with zero uncovered statements. Enabled readiness/full config and empty-read-prefix SQL-error fidelity remain open; delegated review timed out.
* Catalog/help/workflow generation now matches twenty configurations, 34 operation contracts/five workflows and fifteen protocol fixtures. Added guarded configured registration, resources/templates, completion values and asset-publication prompt. Python hides tools from discovery but retains dispatch; registration therefore requires all operation handlers before mutation, and test-only echoes do not increase the real 26-tool subset. HTTP authentication, callback ownership/isolation, fifteen focused race runs and all local Go/reference gates pass with zero uncovered statements. Managed access discovery, remaining service handlers and execute/daemon composition remain open; delegate review timed out.
* Native amd64/ARM64 inventory [35337830552](https://github.com/rcarmo/memento/actions/runs/35337830552) and manifest [35339130259](https://github.com/rcarmo/memento/actions/runs/35339130259) runs passed. Detailed asset metadata run 35340668203 remains in progress.
* Detailed asset metadata expands registration to 26 tools/68 dispatch fixtures. Sixty-eight source cases and seven budget scenarios preserve trash paging, detail/latest validation, version/file ordering, skill-body hashes and metadata-only ZIP handling. Pure-Go creation-time fallback matches native Git linear history; merge/pathspec/rename edges remain explicit. HTTP, twelve concurrent reads, filesystem/object faults, twenty focused races and all local Go/reference gates pass with zero uncovered statements. Configured surfaces/catalog/resources and remaining orchestration are next; delegated review timed out.
* Native amd64/ARM64 archive-lifecycle run [35336891076](https://github.com/rcarmo/memento/actions/runs/35336891076) passed; inventory/manifest runs are still in progress.
* Manifest comparison adds a 25-tool subset/63 dispatch fixtures, with 52 service scenarios, 61 timestamp cases and seven asset summaries. Preserved validation/cap ordering, hash-driven groups, arbitrary-size byte counts, UTC comparison, optional metadata and the second live-policy lookup for inventory. HTTP writer-lock, managed-grant change, twelve concurrent reads, twenty-five focused race repetitions, timestamp fuzz and all local Go/reference gates pass with zero uncovered statements. Richer asset metadata and configured surfaces remain next; source review confirmed the comparison contract.
* Namespace inventory adds a 24-tool subset/58 dispatch fixtures and matches 146 Python cases. Preserved prefix-grant intersections, directory pruning, explicit trash, first-occurrence projections, lexical cursors, page-only parsing and optional body/asset metadata. Registered HTTP reads bypass the writer lock; malformed hidden/off-page content stays unparsed. Sixteen concurrent projections, thirty focused races and all local Go/reference gates pass with zero uncovered statements. Manifest comparison, richer asset metadata and configured surfaces remain next; delegated source review timed out.
* Trash/restore/purge expands the explicit development surface to 23 tools/52 dispatch fixtures. Thirty-one source scenarios/35 steps preserve concept/asset/link bytes, strict confirmation before policy, both-path grants, asset-first purge and revision-bound request hashes. Registered HTTP verifies replay, notifications and Git history after purge; worktree faults, rollback and eight concurrent purges pass with twenty focused race repetitions. All local Go/reference gates pass with zero uncovered statements. Source review confirmed ordering and replay requirements. Configured surfaces/catalog/resources and semantic orchestration remain next.
* Direct create/patch/rename now register a 20-tool subset with 43 dispatch fixtures. Twenty Python scenarios/21 steps pin files, operation rows, request hashes, warning/replay order and postpublication previews. Real HTTP/Git flow, eight concurrent creates, revoked/asset-bound replay, postpublication failures, twenty focused races and all local Go/reference gates pass with zero uncovered statements. Trash/restore/purge, configured surfaces and enabled hot-memory integration remain open; delegated source review timed out.
* Native amd64/ARM64 staging-ticket run [35331384772](https://github.com/rcarmo/memento/actions/runs/35331384772) passed; accepted-read/pruning runs are still in progress.
* Asset pruning matches 29 scenarios/30 steps and expands registration to 17 tools/35 dispatch fixtures. Preserved stored-status asset protection, write-only ID resolution, retention ordering, boolean request hashes, no-op before revision/key checks and status-only notifications. Real Git/HTTP publication, prepublication rollback and eight competing calls pass; all local Go/reference gates and twenty focused race repetitions pass with zero uncovered statements. Source review confirmed retention/no-op/notification details. Configured surfaces/catalog/resources, remaining mutations and daemon integration are next.
* Accepted-asset retrieval matches 80 Python envelopes and expands the development surface to 16 tools/31 dispatch fixtures. Preserved reader/range checks before locking, metadata-only manifest reads, full-container hashes before archive/file slices, source version ordering and revision capture under the lock. Registered HTTP now completes ticket/upload/propose/review/apply/accepted-byte retrieval. Filesystem/metadata/revision failures, thirty focused races and all local Go/reference gates pass with zero uncovered statements. Pruning, configured surfaces and daemon integration remain open; delegated source review timed out.
* Staging MCP begin/status adds a 15-tool development subset with 27 dispatch fixtures and 18 exact envelopes across 21 source steps. Registered HTTP verifies ticket issuance/upload/status/proposal consumption, role changes, spoofing rejection and redacted DB failures. Calls bypass worker admission and the repository lock; direct-wrapper serialisation preserves one-winner concurrent issuance. All local Go/reference gates pass with zero uncovered statements, thirty focused race repetitions and three full service race runs. Configured surfaces, accepted-asset endpoints and daemon integration remain open; delegated review timed out.
* Native amd64/ARM64 corrected reconciliation/tool-registration [35327450803](https://github.com/rcarmo/memento/actions/runs/35327450803) and read-tool [35328519671](https://github.com/rcarmo/memento/actions/runs/35328519671) runs passed. Staging-HTTP run 35329247867 is still in progress.
* Staging auxiliary HTTP route matches 19 Python requests/lifecycle cases: upload-ticket auth bypasses bearer only on POST /assets/staging/upload; normal staging requires proposer credentials. Media/size/replay/expiry/owner checks and no-store/nosniff responses match. Added URL/DB/auth/oversize failures and transport auxiliary-route test with zero uncovered statements. MCP begin/status registration and accepted-asset HTTP retrieval remain open.
* Models-off read/list/search/graph services and registration match 78 envelope/nine malformed-file cases and 20 combined wrapper calls. The explicit development surface has 13 tools. Preserved namespace-only role semantics, readable-bundle ID scans, prefix/trash filtering, timestamp payloads and lexical fallback warnings. HTTP reads complete without the mutation lock; hidden malformed files remain invisible. Zero uncovered statements and ten focused race repetitions pass. Enabled semantic orchestration/configured surfaces/daemon integration remain open; delegate review timed out.
* Native amd64/ARM64 envelopes [35323920298](https://github.com/rcarmo/memento/actions/runs/35323920298) and shielded workers [35324725688](https://github.com/rcarmo/memento/actions/runs/35324725688) passed. Corrected reconciliation/tool-registration run 35327450803 remains in progress.
* Registered the explicit nine-tool proposal/control subset with source-derived metadata and 15 actual wrapper dispatch fixtures. Jobs own SQLite/access/staging handles and share cursor keys; policy resolution happens after writer-lock acquisition. Full registered HTTP lifecycle and queued-policy-change tests pass, including replay, asset reads and resource notifications. Zero uncovered statements and repeated races pass. Configured surfaces/catalog/resources, all other tools and full dynamic argument/diagnostic parity remain open; delegate review timed out.
* Native reconciliation CI [35325423801](https://github.com/rcarmo/memento/actions/runs/35325423801) failed a test-only 1 ms follow-up lookup deadline under load. Restricted that short timeout to the intentionally blocked apply; added safe cleanup/type checks and 100 race repetitions passed. Fixed unfrozen reconciliation setup timestamps; new CI must verify the correction.
* Native amd64/ARM64 lifecycle [35322085366](https://github.com/rcarmo/memento/actions/runs/35322085366) and trusted identity [35323225543](https://github.com/rcarmo/memento/actions/runs/35323225543) passed; later runs need completion checks.
* Operation reconciliation matches 24 source cases: nonblocking writer probe/recheck, principal ownership, control-change permissions, all operation states, scoped replay paths and 500-code-point diagnostics. Added SQL/replay/lock/repository failures with zero uncovered statements. A real timed-out apply reconciles in-progress then committed and replays the original key without another mutation; repeated normal/race checks pass. Worker connection/registry wiring remains next; malformed JSON text and Python replay coercion remain explicit gaps.
* MCP memory-call admission/drain matches nine source scenarios. Normal work owns the admission flag, only operation reconciliation may use the second slot, and timeouts/cancellation do not abort admitted writes. Busy/indeterminate envelopes are exact; panic/cap/drain tests and 30 race repetitions pass with zero uncovered statements. Per-job connection ownership, operation reconciliation and actual registry wiring remain next.
* Service envelopes match 23 exception/nine KeyError-repr/four success-default fixtures. Typed results convert to uMCP ordered JSON without large-integer loss; unknown I/O/runtime failures propagate for transport redaction. Test-only authenticated HTTP submission verifies authorship, structured/text output and denied/unexpected error paths. Zero uncovered statements and repeated races pass. Full endpoint registry/argument validation and broader Python diagnostics remain open.
* Trusted transport identity matches 28 auth/context fixtures and 15 request-authorisation decisions. Preserved managed-token precedence/no fallback, static-name lookup and managed namespace override; transport context cannot be replaced by tool arguments. Added defensive copies, errors/concurrent principal isolation and real HTTP session tests for changed policy, cross-principal binding, admin-tool denial and revocation. Zero uncovered statements and repeated races pass; tool registration/full configuration and response envelopes remain next.
* Native amd64/ARM64 derived content run [35319715037](https://github.com/rcarmo/memento/actions/runs/35319715037) passed; later derived runs still need completion checks.
* Derived lifecycle matches 14 source scenarios: inode-cached migration, invalid-header boundary, transient contention, quarantine/recreated state and explicit rebuild. Recovery files remain inspectable and their contents survive; proposal integration now uses the lifecycle owner. Added all filesystem/DB/sidecar failure branches, strict concurrent waits and collision refusal. Zero uncovered statements and repeated races pass. Owner serialisation/closed handles/collision safety differ explicitly from Python; multi-process and power-loss recovery remain open.
* Native amd64/ARM64 selected revision [35317851386](https://github.com/rcarmo/memento/actions/runs/35317851386) and proposal listing [35318681524](https://github.com/rcarmo/memento/actions/runs/35318681524) passed; newer derived runs need completion checks.
* Graph/status/strict-freshness kernels match 100 graph/four status/five metrics/five wait fixtures. Per-hop ACL filtering, sorted BFS, distinct readable broken targets and scoped orphan checks retain source trash-centre semantics. Added SQL fault injection, malformed-row errors, concurrent revision advancement and cancellation; zero uncovered statements and repeated races pass. Automatic quarantine, index lifecycle and endpoint wiring remain open; focused delegate review timed out.
* Eventual lexical search matches 33 query transformations/56 source pages with exact observed BM25 scores, snippets, filters and offset pagination. SQL namespace/protected/trash filters run before ranking. Added SQL/result/query failures, concurrent update/read isolation and two 10,000-execution fuzz targets; zero uncovered statements and three repeated race runs pass. Strict freshness, semantic fallback, graph/status/quarantine and Python cursor/Unicode-version edges remain open. Delegate review timed out.
* Derived models-off content kernel matches 20 lifecycle steps/four migration fixtures: schema v2/FTS5, rebuild/two-pass updates, complete links/metrics and retained embedding-row maintenance. Source nested per-entry SQLite commits are preserved, including partial state after later parse failure. Failure injection covers every SQL boundary; zero uncovered statements and three race repetitions pass. A real proposal/FTS/archival integration test updates and consumes the Go index. Quarantine, lifecycle caching, search/graph/status and semantic inference orchestration remain open. Delegate confirmed transaction and graph-count pitfalls.
* Native amd64/ARM64 submission [35316428069](https://github.com/rcarmo/memento/actions/runs/35316428069) and proposal reads [35317177003](https://github.com/rcarmo/memento/actions/runs/35317177003) passed; newer list/revise runs need completion checks.
* Proposal list/cursor flow matches 17 Python scenarios with raw-candidate paging, write-visible filtering and revision/policy/status scope binding. Pure-Go Fernet matches four fixed vectors and eleven decoder cases. Added tamper/padding/random/DB failures, concurrent key initialisation, restart invalidation and 10,000 fuzz runs; zero uncovered statements and repeated races pass. Sorted token JSON, lazy key creation and malformed authenticated cursor coercion are documented differences. Endpoint and derived-index integration remain open.
* Native amd64/ARM64 full previews [35314647246](https://github.com/rcarmo/memento/actions/runs/35314647246) and proposal apply [35315397080](https://github.com/rcarmo/memento/actions/runs/35315397080) passed; newer runs need completion checks.
* Selected-change revision matches 25 Python scenarios: clean sorted/deduplicated subsets, required body/asset pairing, original record retention and copied asset bytes/metadata. New records have curator ownership, current base and source-selection metadata. Added insert/asset/COMMIT rollback, missing assets, post-commit preview errors and public Git-backed tests; zero uncovered statements and three race repetitions pass. List/cursor semantics and endpoint wiring remain open.
* Internal get/asset-get matches 21 reference scenarios: proposer/visibility grants, summary/detailed payloads, missing-target fallback, stored-concept ACLs and whole-archive SHA before file slices. Added range/manifest/size/database/history errors and zero-uncovered tests. Fixed nondeterministic existing-asset creation timestamps in submission fixtures. Full endpoint/error envelopes, list/revise and derived-index construction remain open.
* Native amd64/ARM64 stored-asset mutation run [35314059310](https://github.com/rcarmo/memento/actions/runs/35314059310) passed; newer proposal runs need completion checks.
* Non-model proposal preparation/submission matches 33 source scenarios and 99 strict-base64 cases. Inline/staged ZIP checks, body/skill bindings, ownership, rename overlap and active-version conflicts precede atomic proposal/asset/consume writes. Expiry/older-proposal refresh retains source side effects even on later failure. Added SQL/COMMIT/post-commit failures, competing single-winner consumption and full staged-submit-review-apply publication tests. Zero uncovered statements and repeated races pass. Python string coercion/exotic manifest escaping, model drafting and endpoint wiring remain gaps. Delegate checklist timed out.
* Native amd64/ARM64 durable review [35312572689](https://github.com/rcarmo/memento/actions/runs/35312572689) and concept mutations [35313506283](https://github.com/rcarmo/memento/actions/runs/35313506283) passed; later runs need completion checks.
* Internal proposal apply matches 18 source fixtures plus real Git publication, archival freshness, ten competing retries and reopened replay. Existing worktree changes/transaction manager now compose under the repository lock; request metadata and author attribution are retained. Explicit failure tests preserve the source's postpublication operation/proposal split and preview-after-commit errors. Zero uncovered statements and three repeated race runs pass. Derived-index construction, optional hot-memory hooks and endpoint/envelope wiring remain open. Delegate flagged lock scope, but direct inspection of Python @_serialized confirms the same outer lock; no change warranted.
* Native amd64/ARM64 summaries [35311230142](https://github.com/rcarmo/memento/actions/runs/35311230142) and archival inspection [35311892911](https://github.com/rcarmo/memento/actions/runs/35311892911) passed. Later runs still need completion checks.
* Full proposal payloads and previews now match 11 preview/six difflib/three history fixtures, including 50-event descending history, optional nulls, fixed create placeholders and unchanged patch actor/time. Added pure-Go BSD-licensed difflib dependency, Python splitlines boundaries and error coverage. Zero uncovered statements and repeated race tests pass. Apply orchestration remains the next integration step.
* Stored-asset application and existing asset-backed concept adaptation match 14 Python cases, including exact metadata/ZIP bytes, digest ordering and duplicate/missing errors. Aggregate callbacks deduplicate/sort changed paths and publish assets/concepts through the Go transaction manager with replay tests. Source intentionally relies on preparation/staging for ZIP integrity and stored identity checks; apply compares digest fields and manifest shape only. Full Pydantic numeric coercion remains open. Zero uncovered statements and repeated races pass; proposal apply orchestration is next.
* Native amd64/ARM64 same-ID rebase run [35310903740](https://github.com/rcarmo/memento/actions/runs/35310903740) passed; later runs need completion checks.
* Create/patch/rename/trash worktree helpers match 27 Python fixtures with exact bytes, changed paths, directories and partial failures. Added model-copy serialization to preserve patch list order/duplicates/empty values rather than revalidate them; creation retains normal validation. Rooted exclusive/no-follow writes reject symlink escapes/successor collisions. Fault injection, real Git publication/replay/failure isolation and repeated race tests pass with zero uncovered statements. Asset mutation/aggregate apply remain open; delegated code review timed out.
* Durable review matches 56 Python scenarios across approve/reject/request_changes/invalid decisions, including optional keys, stale/conflict rejection without previews, permission checks before replay and archival freshness. Proposal/event/summary/journal changes commit atomically; readback/summary/SQL/COMMIT failure tests verify rollback. Twelve competing calls produce one review, and reopened replay preserves assets while checking current write grants. Zero uncovered statements and three repeated race runs pass. Proposal apply and authenticated endpoint/envelope wiring remain open.
* Native amd64/ARM64 proposal refresh [35309345187](https://github.com/rcarmo/memento/actions/runs/35309345187) and proposal visibility [35309855452](https://github.com/rcarmo/memento/actions/runs/35309855452) passed; newer rebase/summary/archival runs still need completion checks.
* Archival inspection matches 19 source scenarios and 18 stored-report visibility cases. Read-only derived-index freshness, both path grants, destination checks, pre-filter 100-reference cap, retained 100-version asset cap and final revision recheck are implemented. SQL/iteration/filesystem failures and missing-index non-creation have tests; zero uncovered statements and repeated race checks pass. Derived-index creation and archive mutation remain unported. Delegate review identified the documented malformed-report recompute difference; path canonicality is already checked by AuthorizePath before trash construction.
* Proposal summaries match 24 Python cases, including 200-change/asset caps, Unicode text truncation, optional conflicts, unavailable history and first-body asset hash matching. Missing bundle content yields null match status while retaining the asset summary. No preview is required; malformed manifests and unexpected I/O errors propagate. Zero uncovered statements and three repeated race runs pass. Review/apply and HTTP/MCP wiring remain open.
* Native amd64/ARM64 accepted-asset run [35308353113](https://github.com/rcarmo/memento/actions/runs/35308353113) passed; newer proposal runs still need completion checks.
* Internal proposal rebase matches 27 Python scenarios with full proposal/event/operation rows and retained asset bytes/metadata. Role/access checks precede replay; refresh persists separately before subsequent rejection. Rebase preserves ID/author/expiry/patch/assets and atomically clears review fields, records old review values and journals the replay result. Added deferred-COMMIT retry, failure injection and twelve competing calls across two service/database handles. Repository lock is shared with apply/recovery. Zero uncovered statements and repeated race tests pass; MCP envelopes, review/apply and cross-process daemon wiring remain open. Checklist delegate helped identify cases; full code review timed out.
* Native amd64/ARM64 bounded asset and deterministic disconnect regression run [35307821377](https://github.com/rcarmo/memento/actions/runs/35307821377) passed. Later proposal runs are still executing the full Needle corpus.
* Proposal input normalisation, path policy and visibility match 181/483/350 Python cases. Covers optional/null/list fields, trash-only bounds/duplicates, rename destinations, protected namespaces, author/curator ownership and additional review write access. Malformed model input propagates rather than becoming a policy denial, matching the source boundary. Added persisted-read failures, concurrent policy isolation and a 10,000-execution fuzz target. Python-specific input types and malformed diagnostic text remain gaps; no review/rebase/apply endpoint is exposed.
* Proposal conflict/queue refresh matches 63 reference combinations, including all statuses, missing historical bases, asset-only conflicts and strict expiry boundaries. Refresh retains review fields and appends history atomically; tests cover page boundaries/cache isolation, real Git advancement, terminal records, SQL failures and deferred-COMMIT retry. Service package has zero uncovered statements and repeated race checks pass. Non-string path coercion remains a documented difference; visibility/review/rebase/apply and caller serialisation are next. Read-only delegate review timed out without approval.
* Accepted asset repository storage/version helpers ported: exact metadata JSON/timestamps and ZIP bytes, version/kind/latest/retention fixtures, plus size/symlink/special/missing/error paths. Rooted exclusive writes fail closed on symlinks rather than reproduce Python's unsafe write behaviour; partial metadata-before-ZIP failure remains transaction-owned. Service identities/manifests/digest enforcement and HTTP wiring are not added here. Delegate audit confirmed source responsibilities.
* Native amd64/ARM64 transaction recovery [35305238464](https://github.com/rcarmo/memento/actions/runs/35305238464) and access [35306025622](https://github.com/rcarmo/memento/actions/runs/35306025622) CI passed. Fixed staging coverage run still needs native completion.
* Bounded asset retrieval matches 63 read, nine invalid-manifest and 12 malformed-ZIP cases. Complete-file SHA/size checks precede returned slices; explicit CRC fixes Go archive/zip's zero-CRC skip. UTF-8/base64 slices, manifest hashes and limits tested. Accepted asset storage/version resolution and HTTP wiring remain open.
* Staging native run [35307329314](https://github.com/rcarmo/memento/actions/runs/35307329314) failed strict coverage on HTTP disconnect select; ARM64 was cancelled. Added deterministic handshake-triggered context/disconnect tests; 100 ordinary/race repetitions pass. New native CI must verify the fix.
* ZIP validation/staging slice: 52 archive cases and 28 staging/ticket steps match Python, including raw-byte hashes, owner-scoped replay, uploaded-before-expired ticket state, cleared blobs and separate committed expiry cleanup. Added duplicate/count/ratio/magic checks, manifest errors, deferred-COMMIT rollback and concurrent consumption. Stored/deflate/bzip2 supported; LZMA, malformed diagnostics/coercion and HTTP retrieval/upload wiring remain gaps. Delegate audit confirmed expiry/nested transaction details.
* Native amd64/ARM64 SQLite control [35304739414](https://github.com/rcarmo/memento/actions/runs/35304739414) CI passed. Newer access/proposal/recovery commits still need verification.
* Low-level proposal/attached-asset persistence matches nine status/asset fixtures and 45 list queries. Atomic duplicate-asset rollback, explicit caller-owned transactions, nullable-field clearing, expired/unresolved cursor semantics and error paths tested. Deferred-FK regression exposed SQLite transaction leakage after failed database/sql Commit; pinned connection cleanup now protects proposal/access/v5 migration transactions. Service review/rebase/apply and staged/ticket stores remain unported.
* Native amd64/ARM64 Git objects/worktrees [35303715931](https://github.com/rcarmo/memento/actions/runs/35303715931) CI passed. Newer control/access commits still need verification.
* Authorisation/access slice: 630 path-policy cases and 32 managed-access lifecycle steps match Python. Includes synthetic wrapped-key bytes, HMAC digests/tokens, key rotation, persisted audits, last-admin checks and sticky idempotency claims. Rollback/failure/malformed-row and concurrent-revocation tests pass with zero uncovered statements. Actor authorisation/admin endpoint wiring is not implemented; final-admin multi-handle races, full config/coercion and edge decoder parity remain gaps. Delegate audit timed out.
* Native amd64/ARM64 materialisation [35302898870](https://github.com/rcarmo/memento/actions/runs/35302898870) and bootstrap [35304006927](https://github.com/rcarmo/memento/actions/runs/35304006927) CI passed. Later control/recovery commits need verification.
* Transaction apply/replay/startup recovery wired to Go Git/control operations. Nine Python checkpoint fixtures and actual child-process kills before/after publication match expected retry/replay. Tests cover concurrent calls, conflict/DB/Git/derived errors, panic conversion and original operation-ID preservation on failed DB updates. Indeterminate ref durability retains worktree evidence. Non-reentrant locks, generic diagnostic text and non-string replay paths are explicit compatibility gaps; full service/index recovery and power-loss testing remain open.
* Native amd64/ARM64 Git read/CAS [35302645862](https://github.com/rcarmo/memento/actions/runs/35302645862) passed; newer worktree/control/recovery commits still need native verification.
* Pure-Go SQLite control foundation uses modernc.org/sqlite v1.38.2: fourteen Python database migration fixtures and seventeen operation-transition commands match persisted records. V5 asset lift preserves patch hashes, original manifests/blobs and skip-existing semantics; migration DDL/DML ordering matches Python rollback snapshots. WAL/FK/5s busy timeout, concurrent idempotency uniqueness, reopen and injected failures tested. All implemented statement counters covered; transaction recovery and remaining stores unported. Delegate audit confirmed workflow details; no independent code approval claimed.
* Native amd64/ARM64 links/bundles [35301701316](https://github.com/rcarmo/memento/actions/runs/35301701316) CI passed. Later Git commits still need verification.
* Bootstrap implements seeded and empty bare main/current checkout without Git subprocesses. Seeded root commit matches reference SHA, native fsck passes, and injected setup/copy/write/publication failures are covered. Existing main/bootstrap state fail closed; external seed symlinks and Git ignore/filter/config parity remain open. Control-store/transaction recovery is next.
* Native amd64/ARM64 concept [35300997335](https://github.com/rcarmo/memento/actions/runs/35300997335) and writer lease [35301946382](https://github.com/rcarmo/memento/actions/runs/35301946382) CI passed. Later Git commits still require native verification.
* Git object encoding reproduces reference blob/tree/commit hashes and bytes. Native-compatible detached worktree layout and literal exact-path commits pass native Git status/fsck, excluding unrelated files and leaving main unchanged. Injected metadata/index/HEAD failures and lock-ownership races pass with zero uncovered counters. Existing worktrees fail closed instead of source deletion; ignore/pathspec/config/filter/reflog and new-object power-loss parity remain open. Linked-worktree delegate timed out.
* Exact-revision current checkout materialisation implemented using os.Root-confined writes; Python/Git fixture verifies content, executable bits and symlink targets. Tests cover empty trees, stale cache cleanup, path collisions/.git rejection, root overlap, escape attempts and injected filesystem/reader/writer failures. Partial derived caches require retry/reconciliation. Attributes/filters, worktree commit integration and transaction recovery remain open; no canonical ref is changed by materialisation.
* Git read/CAS slice selects go-git v5.16.3 for packed/loose SHA-1 reads, with a custom exclusive `.lock`/rename main publisher. Dependency CAS uses incompatible advisory ref-file locking and is not called. Deterministic packed fixtures, native Git fsck, concurrent Git/Go single-winner CAS and injected I/O failures pass. Post-rename sync failures require original-operation reconciliation. Reflogs/symbolic refs, bootstrap/worktrees/materialization and transaction recovery are still open. Read-only delegate confirmed required workflow boundaries; code-review delegate timed out.
* Native path validation [35299660843](https://github.com/rcarmo/memento/actions/runs/35299660843) passed on amd64/ARM64; later repository CI still needs verification.
* Writer lease ported with source-compatible advisory flock and six owner-contention fixtures. Tests verify concurrent opens, separate-process contention, kill/reacquire, no lock-file unlink and failed setup cleanup. Runtime uses Go/syscalls only; Git publication and SQLite recovery are still unported. Network filesystem flock behaviour is unverified.
* Native amd64/ARM64 sync HTTP [35299013779](https://github.com/rcarmo/memento/actions/runs/35299013779) passed; repository commits still require native completion checks.
* Markdown links/bundle slice: 118 structural/rename documents, 12 scheme and six rename edge cases; ten filesystem scan/audit scenarios and three generated-output datasets. Preserved nested-image destination rewriting and nested-autolink extraction quirk. Pure-Go Goldmark and IDNA dependencies pinned with MIT notices. Broader Markdown/URI corpus, case-fold-equal index ordering and race-safe opens remain gaps. All current statements covered; local fuzz/race tests pass. Delegate review timed out.
* Native amd64/ARM64 transport CLI [35299348132](https://github.com/rcarmo/memento/actions/runs/35299348132) passed; later repository commits still need CI verification.
* Concept metadata/body and frontmatter slice: 353 schema, nine body, 145 parse and 253 exact serialization cases. Pure-Go YAML node dependency pinned; MIT notices retained. Preserved default-status enum serialization failure and negative-float timestamp quirks. Tested errors, alias expansion cap, long scalar lines and repeated races. Full YAML key/type/graph and wide double-quoted parity remain gaps; links/bundles/transactions and service integration are unported. Delegate inspection timed out.
* Native amd64/ARM64 async raw HTTP [35297974705](https://github.com/rcarmo/memento/actions/runs/35297974705) and notification [35298219097](https://github.com/rcarmo/memento/actions/runs/35298219097) runs passed. Later commit CI still needs verification.
* Repository path foundation ports canonical validation, reserved classification and filesystem checks with 37 synthetic baseline cases. Added symlink/FIFO/non-directory/root tests, injected OS failures and pathlib-preserving root normalisation; filepath.Clean would incorrectly collapse symlink/.. roots. No read/write service operations are wired; transaction/open race containment, ACLs and recovery remain open.
* uMCP transport args/dispatch match 70 sync/async reference cases; file/stdio plus all six live network combinations pass. Added shared stdio loop, serialised output, announcements, cancellation and sink reset between runs. Typed constructors still reject nonsensical HTTP limits/endpoints earlier than Python; hostname multi-bind, platform diagnostics and Memento operational CLI are open. No service endpoints or deployment changed.
* Synchronous raw parser/listener implemented against CPython 3.13.14: 218 parser and 218 wire cases pass. Covers folded/duplicate/malformed headers, route-dependent body reads, HTTP/0.9, Continue, exact HTML errors and connection limits. Forty-one HTTP scenarios run through net/http plus both raw listeners; real sync/async SSE and body-timeout tests pass. Fixed ignored continuation handling after empty field/Unix From lines. PSF licence retained. All statement counters covered, ten repeated race runs pass. Transport CLI, stdlib-version compatibility and broader service integration are open; delegated review timed out.
* Notification routing/fallback matches 72 sync/async source cases. File/TCP/default notifications use configurable serialised stdout; legacy SSE falls back when no targeted delivery succeeds; active Streamable HTTP suppresses fallback. Tested concurrent lines and failed writes. Multi-transport multiplexing is not exposed by the reference CLI and is not added here.
* Native amd64/ARM64 file/TCP commit CI [35296204243](https://github.com/rcarmo/memento/actions/runs/35296204243) passed. SSE and async raw HTTP commits need separate native completion checks.
* Async raw HTTP parsers and connection/listener implemented: 132 byte-stream cases, 16 sync/async auth-order cases and all 41 prior HTTP scenarios pass. Added pipelining, request-count limits, read deadlines, cancellation, real SSE and flush-error tests. Statement counters are all covered and repeated race tests pass. The synchronous raw parser/HTML errors, CLI/stdout fallback and full service integration remain open. Delegate parser audit timed out; no independent approval.
* Native amd64/ARM64 HTTP commit CI [35295158648](https://github.com/rcarmo/memento/actions/runs/35295158648) passed, including the full Needle corpus. Newer transport commits need their own native results.
* Legacy SSE handlers match 68 live request/event captures plus handshakes from the Python references. Sync unbounded queues and async write acknowledgement are separate modes. Tests cover principal binding, subscriptions, reconnect, queue closure, write/flush failure and repeated races; no uncovered statements. Raw HTTP server/parser limits, unsupported-method HTML, notification stdout fallback and operational CLI integration remain open. Latest HTTP/TCP native runs were still running at this checkpoint.
* File mode and TCP implemented with seven file cases and 34 live TCP reference cases. Sync/async UTF-8 and line-limit differences are explicit; sync timeout applies per blocking I/O call, excluding handler time. Repeated race tests cover concurrent peer contexts and cancellation; all implemented statements are covered. Legacy SSE, raw HTTP parsing and CLI integration are the next transport gates. Independent TCP review timed out.
* Native amd64 and ARM64 [CI 35291886039](https://github.com/rcarmo/memento/actions/runs/35291886039) both passed the full 360-case Needle gate for `3b240b0`. No SIMD optimisation enabled.
* Streamable HTTP implemented with UUID sessions, 100-message queues, principal/version binding, TTL/caps, event streams and deletion. Forty-one actual Python sync/async HTTP scenarios match; local statement/race tests cover disconnect and I/O failures. Corrected source-specific auxiliary route auth ordering and session-version JSON errors. Raw parser/connection-limit and legacy transport parity remain pending; delegate review timed out.
* Added uMCP completion/provider resolution, config/initialise composition, logging and redaction with sync/async fixtures and full statement tests. Native ARM64 job for worker/inference commit `3b240b0` completed successfully, including the 360-case corpus; amd64 still running at this checkpoint.
* Added dynamic resource/template and prompt methods with sync/async differential fixtures, metadata isolation, pagination, binary/nested content, subscription state and concurrency/error tests. All current Go statements covered. Completion, logging, server composition and network/session transports remain gaps.
* Added dynamic uMCP tool registration/list/call, signature coercion, schema-subset validation and ordered Python-compatible result text. Oracle covers 380 formatting/schema and 40 call cases in both sync/async references. Focused review found missing panic containment; added remote-redacted recovery. Strict coercion suggestions deliberately not applied because they change source behaviour; service validators remain an open requirement.
* Added the Go embedding-worker executable and framed protocol, with Rust response fixtures, short/partial I/O, bounds and nonfinite-output tests. Unchanged Python client accepts five real GTE vectors and model identity; local coverage/race/fuzz/cross gates pass. Malformed JSON/duplicate-key fidelity and host worker integration remain incomplete.
* Inference commit `e730023` CI stopped during timed vector fuzz shutdown (`context deadline exceeded`, no failing corpus input); ARM job cancelled before corpus completion. Replaced the five-second fuzz budget with 10,000 executions plus a separate safety timeout. Local full 360-case Needle match remains valid; native full-corpus verification must be rerun.
* Scalar Needle encoder/decoder, RoPE, grouped attention, KV caches, norms/gates and constrained decoding implemented. Local full corpus comparison matches 360/360 complete Rust output strings (1408.79 seconds), with synthetic branch/error/cancellation tests reaching 100% coverage. Native corpus CI added as a required gate; service routing expansion remains unported.
* SentencePiece commit `727fe9f` passed native CI [35288453919](https://github.com/rcarmo/memento/actions/runs/35288453919). No SIMD/native runtime or production change.

### 2026-09-17

* Added Apache-2.0-derived pure-Go SentencePiece protobuf/model parsing, BPE/Unigram, charsmap/whitespace normalisation and byte decode, plus Needle special-token handling. All implemented statements covered; local race/fuzz/cross pass. 288 synthetic oracle cases and 15 real encode/decode cases plus 8192 token decodes match Rust. Encoder/decoder and constrained generation are next.
* Needle NDL1 parser implemented with section/metadata checksums, tensor bounds and BF16 expansion; the real-model oracle matches 31 tensors and 8192 pieces. Local parser tests reach 100% statement coverage. Encoding/inference and exact malformed-diagnostic parity remain open. GTE commit `86bd39e` passed native CI [35286859612](https://github.com/rcarmo/memento/actions/runs/35286859612), including real-model parity on both architectures.
* Restored scalar Go GTE1 loading/tokenisation/transformer inference, based on original Go GTE algorithms with Memento Unicode and cancellation compatibility. All implemented Go statements covered; check/race/fuzz/cross pass. Public GTE1 SHA verified; five real model inputs match exact token IDs with local max_abs<=1.2e-7 against Rust. Native model-parity CI added; mmap/resource and worker integration still pending. URL helper commit `b0be94e` passed CI [35285682108](https://github.com/rcarmo/memento/actions/runs/35285682108).
* Added URL/Origin helpers with Python-generated allowlist, loopback, IPv6/port, raw path and Unicode NFKC delimiter fixtures. `golang.org/x/text v0.23.0` is a pure-Go dependency; no native runtime introduced. Local gates remain at 100% coverage. Stdio commit `b38896d` passed native CI [35285053975](https://github.com/rcarmo/memento/actions/runs/35285053975).
* Added sequential stdio framing with actual sync/async transport-loop fixtures, invalid UTF-8 handling, notification suppression and large-line tests. Local coverage/race/fuzz/cross gates pass at 100%; pagination commit `fc7a78e` also passed native CI [35284597442](https://github.com/rcarmo/memento/actions/runs/35284597442).
* Discovery pagination now matches 51 pinned sync/async cases, including principal/list binding and exact opaque bytes. Local check/race/fuzz/cross gates pass at 100% statement coverage. Dispatch/progress slice `877738c` passed native CI [35283952029](https://github.com/rcarmo/memento/actions/runs/35283952029).
* Continued uMCP port with HTTP helper rules, dispatcher validation/context, cooperative cancellation and progress; oracle runs both Python sync/async bases. Fixed exact large-integer progress comparisons and Python/Go status-phrase differences exposed by fixtures. Origin/URL rules, discovery and real transports remain incomplete.
* Initial scaffold committed/pushed as `b654c892c75f1aa436a988ff905ec9b60e5bea51`; native amd64 and ARM64 [Go port CI 35282374718](https://github.com/rcarmo/memento/actions/runs/35282374718) passed coverage, race, fuzz and static cross-build gates. This completes branch preparation, not the end-to-end port. Next: remaining uMCP shared rules and transport/session conformance.
* Created branch `go` at Memento `0b0b8f94dd8b0410a0e3c0fd547e995d2b739b41` in `/workspace/projects/memento-go`.
* Pinned uMCP Git tip `30cce7dfe08c6ee63de235f7d81754ba286dafbb`; difference from deployed `9c89a70` is documentation-only.
* Added scalar vector primitives, response envelopes, uMCP ID/response/version helpers, development-only CLI and oracle fixtures. Each current package reached 100% statement coverage locally; service/model subsystems remain unported.
* User explicitly requested full coverage; added raw zero-uncovered-block enforcement rather than trusting the rounded display percentage.
* Validation: all 17 current production Go functions at 100% statement coverage; race tests pass; uMCP fuzz ran about 134k executions and vector fuzz about 636k; baseline amd64/ARM64 binaries are statically linked. Oracle fixtures regenerate byte-identically. Original Python/Rust `make check` and `make typecheck` pass from this worktree.
* Oracle Python script passes Ruff/format/mypy independently. New documentation file links resolve. Delegated scope audit timed out; no independent approval claimed.
* Quality: ★★★★☆ 8/10 (problem 2, scope 2, test 2, dependencies 0, risk 2). Git/SQLite/SentencePiece/ABI decisions are explicit open gates, not blockers to the independent foundations.

## Notes

Refinement: full end-to-end service, scalar first, CGO-free runtime, current data/model/wire contracts, at least baseline Linux amd64 and ARM64. Keep frontend assets; do not mistake browser JavaScript for a backend dependency. Old implementations are test-only references until retirement. Full correctness is a compatibility objective, not a claim of a mathematical proof from test coverage. No release/deployment is requested in this preparation step.

## Links

* [Go port index](../../docs/go-port/README.md)
* [Architecture and phases](../../docs/go-port/plan.md)
* [Test gates](../../docs/go-port/testing.md)
